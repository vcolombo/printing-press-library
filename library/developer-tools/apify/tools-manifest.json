{
  "api_name": "apify",
  "base_url": "https://api.apify.com",
  "description": "The Apify API (version 2) provides programmatic access to the [Apify\nplatform](https://docs.apify.com). The API is organized\naround [RESTful](https://en.wikipedia.org/wiki/Representational_state_transfer)\nHTTP endpoints.\n\nYou can download the complete OpenAPI schema of Apify API in the [YAML](http://docs.apify.com/api/openapi.yaml) or [JSON](http://docs.apify.com/api/openapi.json) formats. The source code is also available on [GitHub](https://github.com/apify/apify-docs/tree/master/apify-api/openapi).\n\nAll requests and responses (including errors) are encoded in\n[JSON](http://www.json.org/) format with UTF-8 encoding,\nwith a few exceptions that are explicitly described in the reference.\n\n- To access the API using [Node.js](https://nodejs.org/en/), we recommend the [`apify-client`](https://docs.apify.com/api/client/js) [NPM\npackage](https://www.npmjs.com/package/apify-client).\n- To access the API using [Python](https://www.python.org/), we recommend the [`apify-client`](https://docs.apify.com/api/client/python) [PyPI\npackage](https://pypi.org/project/apify-client/).\n\nThe clients' functions correspond to the API endpoints and have the same\nparameters. This simplifies development of apps that depend on the Apify\nplatform.\n\n:::note Important Request Details\n\n- `Content-Type` header: For requests with a JSON body, you must include the `Content-Type: application/json` header.\n\n- Method override: You can override the HTTP method using the `method` query parameter. This is useful for clients that can only send `GET` requests. For example, to call a `POST` endpoint, append `?method=POST` to the URL of your `GET` request.\n\n:::\n\n## Authentication\n\u003cspan id=\"/introduction/authentication\"\u003e\u003c/span\u003e\n\n**You can find your API token on the\n[Integrations](https://console.apify.com/settings/integrations) page in the\nApify Console.**\n\nTo use your token in a request, either:\n\n- Add the token to your request's `Authorization` header as `Bearer \u003ctoken\u003e`.\nE.g., `Authorization: Bearer xxxxxxx`.\n[More info](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Authorization).\n(Recommended).\n- Add it as the `token` parameter to your request URL. (Less secure).\n\nUsing your token in the request header is more secure than using it as a URL\nparameter because URLs are often stored\nin browser history and server logs. This creates a chance for someone\nunauthorized to access your API token.\n\n**Never share your API token or password with untrusted parties!**\n\nFor more information, see our\n[integrations](https://docs.apify.com/platform/integrations) documentation.\n\n## Basic usage\n\u003cspan id=\"/introduction/basic-usage\"\u003e\u003c/span\u003e\n\nTo run an Actor, send a POST request to the [Run\nActor](#/reference/actors/run-collection/run-actor) endpoint using either the\nActor ID code (e.g. `vKg4IjxZbEYTYeW8T`) or its name (e.g.\n`janedoe~my-actor`):\n\n`https://api.apify.com/v2/acts/[actor_id]/runs`\n\nIf the Actor is not runnable anonymously, you will receive a 401 or 403\n[response code](https://developer.mozilla.org/en-US/docs/Web/HTTP/Status).\nThis means you need to add your [secret API\ntoken](https://console.apify.com/account#/integrations) to the request's\n`Authorization` header ([recommended](#/introduction/authentication)) or as a\nURL query parameter `?token=[your_token]` (less secure).\n\nOptionally, you can include the query parameters described in the [Run\nActor](#/reference/actors/run-collection/run-actor) section to customize your\nrun.\n\nIf you're using Node.js, the best way to run an Actor is using the\n`Apify.call()` method from the [Apify\nSDK](https://sdk.apify.com/docs/api/apify#apifycallactid-input-options). It\nruns the Actor using the account you are currently logged into (determined\nby the [secret API token](https://console.apify.com/account#/integrations)).\nThe result is an [Actor run\nobject](https://sdk.apify.com/docs/typedefs/actor-run) and its output (if\nany).\n\nA typical workflow is as follows:\n\n1. Run an Actor or task using the [Run\nActor](#/reference/actors/run-collection/run-actor) or [Run\ntask](#/reference/actor-tasks/run-collection/run-task) API endpoints.\n2. Monitor the Actor run by periodically polling its progress using the [Get\nrun](#/reference/actor-runs/run-object-and-its-storages/get-run) API\nendpoint.\n3. Fetch the results from the [Get\nitems](#/reference/datasets/item-collection/get-items) API endpoint using the\n`defaultDatasetId`, which you receive in the Run request response.\nAdditional data may be stored in a key-value store. You can fetch them from\nthe [Get record](#/reference/key-value-stores/record/get-record) API endpoint\nusing the `defaultKeyValueStoreId` and the store's `key`.\n\n**Note**: Instead of periodic polling, you can also run your\n[Actor](#/reference/actors/run-actor-synchronously) or\n[task](#/reference/actor-tasks/runs-collection/run-task-synchronously)\nsynchronously. This will ensure that the request waits for 300 seconds (5\nminutes) for the run to finish and returns its output. If the run takes\nlonger, the request will time out and throw an error.\n\n## Response structure\n\u003cspan id=\"/introduction/response-structure\"\u003e\u003c/span\u003e\n\nMost API endpoints return a JSON object with the `data` property:\n\n```\n{\n    \"data\": {\n        ...\n    }\n}\n```\n\nHowever, there are a few explicitly described exceptions, such as\n[Get dataset items](#/reference/datasets/item-collection/get-items) or\nKey-value store [Get record](#/reference/key-value-stores/record/get-record)\nAPI endpoints, which return data in other formats.\nIn case of an error, the response has the HTTP status code in the range of\n4xx or 5xx and the `data` property is replaced with `error`. For example:\n\n```\n{\n    \"error\": {\n        \"type\": \"record-not-found\",\n        \"message\": \"Store was not found.\"\n    }\n}\n```\n\nSee [Errors](#/introduction/errors) for more details.\n\n## Pagination\n\u003cspan id=\"/introduction/pagination\"\u003e\u003c/span\u003e\n\nAll API endpoints that return a list of records\n(e.g. [Get list of\nActors](#/reference/actors/actor-collection/get-list-of-actors))\nenforce pagination in order to limit the size of their responses.\n\nMost of these API endpoints are paginated using the `offset` and `limit`\nquery parameters.\nThe only exception is [Get list of\nkeys](#/reference/key-value-stores/key-collection/get-list-of-keys),\nwhich is paginated using the `exclusiveStartKey` query parameter.\n\n**IMPORTANT**: Each API endpoint that supports pagination enforces a certain\nmaximum value for the `limit` parameter,\nin order to reduce the load on Apify servers.\nThe maximum limit could change in future so you should never\nrely on a specific value and check the responses of these API endpoints.\n\n### Using offset\n\u003cspan id=\"/introduction/pagination/using-offset\"\u003e\u003c/span\u003e\n\nMost API endpoints that return a list of records enable pagination using the\nfollowing query parameters:\n\n\u003ctable\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003elimit\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eLimits the response to contain a specific maximum number of items, e.g. \u003ccode\u003elimit=20\u003c/code\u003e.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003eoffset\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eSkips a number of items from the beginning of the list, e.g. \u003ccode\u003eoffset=100\u003c/code\u003e.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003edesc\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\n    By default, items are sorted in the order in which they were created or added to the list.\n    This feature is useful when fetching all the items, because it ensures that items\n    created after the client started the pagination will not be skipped.\n    If you specify the \u003ccode\u003edesc=1\u003c/code\u003e parameter, the items will be returned in the reverse order,\n    i.e. from the newest to the oldest items.\n    \u003c/td\u003e\n  \u003c/tr\u003e\n\u003c/table\u003e\n\nThe response of these API endpoints is always a JSON object with the\nfollowing structure:\n\n```\n{\n    \"data\": {\n        \"total\": 2560,\n        \"offset\": 250,\n        \"limit\": 1000,\n        \"count\": 1000,\n        \"desc\": false,\n        \"items\": [\n            { 1st object },\n            { 2nd object },\n            ...\n            { 1000th object }\n        ]\n    }\n}\n```\n\nThe following table describes the meaning of the response properties:\n\n\u003ctable\u003e\n  \u003ctr\u003e\n    \u003cth\u003eProperty\u003c/th\u003e\n    \u003cth\u003eDescription\u003c/th\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003etotal\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThe total number of items available in the list.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003eoffset\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThe number of items that were skipped at the start.\n    This is equal to the \u003ccode\u003eoffset\u003c/code\u003e query parameter if it was provided, otherwise it is \u003ccode\u003e0\u003c/code\u003e.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003elimit\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThe maximum number of items that can be returned in the HTTP response.\n    It equals to the \u003ccode\u003elimit\u003c/code\u003e query parameter if it was provided or\n    the maximum limit enforced for the particular API endpoint, whichever is smaller.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003ecount\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThe actual number of items returned in the HTTP response.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003edesc\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003etrue\u003c/code\u003e if data were requested in descending order and \u003ccode\u003efalse\u003c/code\u003e otherwise.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003eitems\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eAn array of requested items.\u003c/td\u003e\n  \u003c/tr\u003e\n\u003c/table\u003e\n\n### Using key\n\u003cspan id=\"/introduction/pagination/using-key\"\u003e\u003c/span\u003e\n\nThe records in the [key-value\nstore](https://docs.apify.com/platform/storage/key-value-store)\nare not ordered based on numerical indexes,\nbut rather by their keys in the UTF-8 binary order.\nTherefore the [Get list of\nkeys](#/reference/key-value-stores/key-collection/get-list-of-keys)\nAPI endpoint only supports pagination using the following query parameters:\n\n\u003ctable\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003elimit\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eLimits the response to contain a specific maximum number items, e.g. \u003ccode\u003elimit=20\u003c/code\u003e.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003eexclusiveStartKey\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eSkips all records with keys up to the given key including the given key,\n    in the UTF-8 binary order.\u003c/td\u003e\n  \u003c/tr\u003e\n\u003c/table\u003e\n\nThe response of the API endpoint is always a JSON object with following\nstructure:\n\n```\n{\n    \"data\": {\n        \"limit\": 1000,\n        \"isTruncated\": true,\n        \"exclusiveStartKey\": \"my-key\",\n        \"nextExclusiveStartKey\": \"some-other-key\",\n        \"items\": [\n            { 1st object },\n            { 2nd object },\n            ...\n            { 1000th object }\n        ]\n    }\n}\n```\n\nThe following table describes the meaning of the response properties:\n\n\u003ctable\u003e\n  \u003ctr\u003e\n    \u003cth\u003eProperty\u003c/th\u003e\n    \u003cth\u003eDescription\u003c/th\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003elimit\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThe maximum number of items that can be returned in the HTTP response.\n    It equals to the \u003ccode\u003elimit\u003c/code\u003e query parameter if it was provided or\n    the maximum limit enforced for the particular endpoint, whichever is smaller.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003eisTruncated\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003etrue\u003c/code\u003e if there are more items left to be queried. Otherwise \u003ccode\u003efalse\u003c/code\u003e.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003eexclusiveStartKey\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThe last key that was skipped at the start. Is `null` for the first page.\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003enextExclusiveStartKey\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThe value for the \u003ccode\u003eexclusiveStartKey\u003c/code\u003e parameter to query the next page of items.\u003c/td\u003e\n  \u003c/tr\u003e\n\u003c/table\u003e\n\n## Errors\n\u003cspan id=\"/introduction/errors\"\u003e\u003c/span\u003e\n\nThe Apify API uses common HTTP status codes: `2xx` range for success, `4xx`\nrange for errors caused by the caller\n(invalid requests) and `5xx` range for server errors (these are rare).\nEach error response contains a JSON object defining the `error` property,\nwhich is an object with\nthe `type` and `message` properties that contain the error code and a\nhuman-readable error description, respectively.\n\nFor example:\n\n```\n{\n    \"error\": {\n        \"type\": \"record-not-found\",\n        \"message\": \"Store was not found.\"\n    }\n}\n```\n\nHere is the table of the most common errors that can occur for many API\nendpoints:\n\n\u003ctable\u003e\n  \u003ctr\u003e\n    \u003cth\u003estatus\u003c/th\u003e\n    \u003cth\u003etype\u003c/th\u003e\n    \u003cth\u003emessage\u003c/th\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003e400\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003einvalid-request\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003ePOST data must be a JSON object\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003e400\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003einvalid-value\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eInvalid value provided: Comments required\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003e400\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003einvalid-record-key\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eRecord key contains invalid character\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003e401\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003etoken-not-provided\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eAuthentication token was not provided\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003e404\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003erecord-not-found\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eStore was not found\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003e429\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003erate-limit-exceeded\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eYou have exceeded the rate limit of ... requests per second\u003c/td\u003e\n  \u003c/tr\u003e\n  \u003ctr\u003e\n    \u003ctd\u003e\u003ccode\u003e405\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003e\u003ccode\u003emethod-not-allowed\u003c/code\u003e\u003c/td\u003e\n    \u003ctd\u003eThis API endpoint can only be accessed using the following HTTP methods: OPTIONS, POST\u003c/td\u003e\n  \u003c/tr\u003e\n\u003c/table\u003e\n\n## Rate limiting\n\u003cspan id=\"/introduction/rate-limiting\"\u003e\u003c/span\u003e\n\nAll API endpoints limit the rate of requests in order to prevent overloading of Apify servers by misbehaving clients.\n\nThere are two kinds of rate limits - a global rate limit and a per-resource rate limit.\n\n### Global rate limit\n\u003cspan id=\"/introduction/rate-limiting/global-rate-limit\"\u003e\u003c/span\u003e\n\nThe global rate limit is set to _250 000 requests per minute_.\nFor [authenticated](#/introduction/authentication) requests, it is counted per user,\nand for unauthenticated requests, it is counted per IP address.\n\n### Per-resource rate limit\n\u003cspan id=\"/introduction/rate-limiting/per-resource-rate-limit\"\u003e\u003c/span\u003e\n\nThe default per-resource rate limit is _60 requests per second per resource_, which in this context means a single Actor, a single Actor run, a single dataset, single key-value store etc.\nThe default rate limit is applied to every API endpoint except a few select ones, which have higher rate limits.\nEach API endpoint returns its rate limit in `X-RateLimit-Limit` header.\n\nThese endpoints have a rate limit of _200 requests per second per resource_:\n\n* CRUD ([get](#/reference/key-value-stores/record/get-record),\n  [put](#/reference/key-value-stores/record/put-record),\n  [delete](#/reference/key-value-stores/record/delete-record))\n  operations on key-value store records\n\nThese endpoints have a rate limit of _400 requests per second per resource_:\n* [Run Actor](#/reference/actors/run-collection/run-actor)\n* [Run Actor task asynchronously](#/reference/actor-tasks/runs-collection/run-task-asynchronously)\n* [Run Actor task synchronously](#/reference/actor-tasks/runs-collection/run-task-synchronously)\n* [Metamorph Actor run](#/reference/actors/metamorph-run/metamorph-run)\n* [Push items](#/reference/datasets/item-collection/put-items) to dataset\n* CRUD\n  ([add](#/reference/request-queues/request-collection/add-request),\n  [get](#/reference/request-queues/request-collection/get-request),\n  [update](#/reference/request-queues/request-collection/update-request),\n  [delete](#/reference/request-queues/request-collection/delete-request))\n  operations on requests in request queues\n\n### Rate limit exceeded errors\n\u003cspan id=\"/introduction/rate-limiting/rate-limit-exceeded-errors\"\u003e\u003c/span\u003e\n\nIf the client is sending too many requests, the API endpoints respond with the HTTP status code `429 Too Many Requests`\nand the following body:\n\n```\n{\n    \"error\": {\n        \"type\": \"rate-limit-exceeded\",\n        \"message\": \"You have exceeded the rate limit of ... requests per second\"\n    }\n}\n```\n\n### Retrying rate-limited requests with exponential backoff\n\u003cspan id=\"/introduction/rate-limiting/retrying-rate-limited-requests-with-exponential-backoff\"\u003e\u003c/span\u003e\n\nIf the client receives the rate limit error, it should wait a certain period of time and then retry the request.\nIf the error happens again, the client should double the wait period and retry the request,\nand so on. This algorithm is known as _exponential backoff_\nand it can be described using the following pseudo-code:\n\n1. Define a variable `DELAY=500`\n2. Send the HTTP request to the API endpoint\n3. If the response has status code not equal to `429` then you are done. Otherwise:\n   * Wait for a period of time chosen randomly from the interval `DELAY` to `2*DELAY` milliseconds\n   * Double the future wait period by setting `DELAY = 2*DELAY`\n   * Continue with step 2\n\nIf all requests sent by the client implement the above steps,\nthe client will automatically use the maximum available bandwidth for its requests.\n\nNote that the Apify API clients [for JavaScript](https://docs.apify.com/api/client/js)\nand [for Python](https://docs.apify.com/api/client/python)\nuse the exponential backoff algorithm transparently, so that you do not need to worry about it.\n\n## Referring to resources\n\u003cspan id=\"/introduction/referring-to-resources\"\u003e\u003c/span\u003e\n\nThere are three main ways to refer to a resource you're accessing via API.\n\n- the resource ID (e.g. `iKkPcIgVvwmztduf8`)\n- `username~resourcename` - when using this access method, you will need to\nuse your API token, and access will only work if you have the correct\npermissions.\n- `~resourcename` - for this, you need to use an API token, and the\n`resourcename` refers to a resource in the API token owner's account.",
  "mcp_ready": "full",
  "http_transport": "standard",
  "auth": {
    "type": "bearer_token",
    "header": "Authorization",
    "env_vars": [
      "APIFY_TOKEN"
    ],
    "env_var_specs": [
      {
        "name": "APIFY_TOKEN",
        "kind": "per_call",
        "required": true,
        "sensitive": true,
        "inferred": true
      }
    ],
    "key_url": "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Authorization"
  },
  "required_headers": [],
  "tools": [
    {
      "name": "actor-builds_delete",
      "description": "Delete the build. The build that is the current default build for the Actor cannot be deleted. Only users with build permissions for the Actor can delete builds. Required: buildId. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-builds/{buildId}",
      "params": [
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-builds_get",
      "description": "Gets a list of all builds for a user. The response is a JSON array of objects, where each object contains basic information about a single build. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `startedAt` field in ascending order. Therefore, you can use pagination to incrementally fetch all builds while new ones are still being started. To sort the records in descending order, use the `desc=1` parameter. Optional: offset, limit, desc. Returns the ListOfBuildsResponse.",
      "method": "GET",
      "path": "/v2/actor-builds",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order."
        }
      ]
    },
    {
      "name": "actor-builds_get-actorbuilds",
      "description": "Gets an object that contains all the details about a specific build of an Actor. By passing the optional `waitForFinish` parameter the API endpoint will synchronously wait for the build to finish. This is useful to avoid periodic polling when waiting for an Actor build to finish. This endpoint does not require the authentication token. Instead, calls are authenticated using a hard-to-guess ID of the build. However, if you access the endpoint without the token, certain attributes, such as `usageUsd` and `usageTotalUsd`, will be hidden. Required: buildId. Optional: waitForFinish. Returns the ActorBuildsGetActorbuildsResponse. (public)",
      "method": "GET",
      "path": "/v2/actor-builds/{buildId}",
      "no_auth": true,
      "params": [
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab.",
          "required": true
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the build to finish."
        }
      ]
    },
    {
      "name": "actor-builds_abort_actor-build-post",
      "description": "Aborts an Actor build and returns an object that contains all the details about the build. Only builds that are starting or running are aborted. For builds with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing. Required: buildId.",
      "method": "POST",
      "path": "/v2/actor-builds/{buildId}/abort",
      "params": [
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-builds_log_actor-build-get",
      "description": "Retrieves Actor build's logs. This endpoint is a shortcut for getting the build's log. Same as [Get log](/api/v2/log-get) endpoint. Required: buildId. Optional: stream, download.",
      "method": "GET",
      "path": "/v2/actor-builds/{buildId}/log",
      "params": [
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab.",
          "required": true
        },
        {
          "name": "stream",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
        },
        {
          "name": "download",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
        }
      ]
    },
    {
      "name": "actor-builds_openapi-json_actor-build-get",
      "description": "Get the OpenAPI definition for Actor builds. Two similar endpoints are available: - [First endpoint](/api/v2/act-openapi-json-get): Requires both `actorId` and `buildId`. Use `default` as the `buildId` to get the OpenAPI schema for the default Actor build. - [Second endpoint](/api/v2/actor-build-openapi-json-get): Requires only `buildId`. Get the OpenAPI definition for a specific Actor build. Authentication is based on the build's unique ID. No authentication token is required. :::note You can also use the [`/api/v2/act-openapi-json-get`](/api/v2/act-openapi-json-get) endpoint to get the OpenAPI definition for a build. :::. Required: buildId. Returns the OpenapiJsonActorBuildGetResponse. (public)",
      "method": "GET",
      "path": "/v2/actor-builds/{buildId}/openapi.json",
      "no_auth": true,
      "params": [
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab. Use the special value `default` to get the OpenAPI schema for the Actor's default build.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_delete",
      "description": "Delete the run. Only finished runs can be deleted. Only the person or organization that initiated the run can delete it. Required: runId. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_get",
      "description": "Gets a list of all runs for a user. The response is a list of objects, where each object contains basic information about a single Actor run. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 array elements. By default, the records are sorted by the `startedAt` field in ascending order. Therefore, you can use pagination to incrementally fetch all records while new ones are still being created. To sort the records in descending order, use `desc=1` parameter. You can also filter runs by `startedAt`` and `status`` fields ([available statuses](https://docs.apify.com/platform/actors/running/runs-and-builds#lifecycle)). Optional: offset, limit, desc (plus 3 more). Returns the ActorRunsGetResponse.",
      "method": "GET",
      "path": "/v2/actor-runs",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order."
        },
        {
          "name": "status",
          "type": "array",
          "location": "query",
          "description": "Single status or comma-separated list of statuses, see ([available statuses](https://docs."
        },
        {
          "name": "startedAfter",
          "type": "string",
          "location": "query",
          "description": "Filter runs that started after the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
        },
        {
          "name": "startedBefore",
          "type": "string",
          "location": "query",
          "description": "Filter runs that started before the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
        }
      ]
    },
    {
      "name": "actor-runs_get-actorruns",
      "description": "This is not a single endpoint, but an entire group of endpoints that lets you retrieve the run or any of its default storages. ##### Convenience endpoints for Actor run default storages * [Dataset](/api/v2/default-dataset) * [Key-value store](/api/v2/default-key-value-store) * [Request queue](/api/v2/default-request-queue) Gets an object that contains all the details about a specific run of an Actor. By passing the optional `waitForFinish` parameter the API endpoint will synchronously wait for the run to finish. This is useful to avoid periodic polling when waiting for Actor run to complete. Note that the first response after completion can still show preliminary `stats`, costs, and event counts. For stable figures, wait about 10 seconds and call the endpoint again. This endpoint does not require the authentication token. Instead, calls are authenticated using a hard-to-guess ID of the run. However, if you access the endpoint without the token, certain attributes, such as `usageUsd` and `usageTotalUsd`, will be hidden. Required: runId. Optional: waitForFinish. Returns the ActorRunsGetActorrunsResponse.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the run to finish."
        }
      ]
    },
    {
      "name": "actor-runs_put",
      "description": "This endpoint can be used to update both the run's status message and to configure its general resource access level. **Status message:** You can set a single status message on your run that will be displayed in the Apify Console UI. During an Actor run, you will typically do this in order to inform users of your Actor about the Actor's progress. The request body must contain `runId` and `statusMessage` properties. The `isStatusMessageTerminal` property is optional and it indicates if the status message is the very last one. In the absence of a status message, the platform will try to substitute sensible defaults. **General resource access:** You can also update the run's general resource access setting, which determines who can view the run and its related data. Allowed values: * `FOLLOW_USER_SETTING` - The run inherits the general access setting from the account level. * `ANYONE_WITH_ID_CAN_READ` - The run can be viewed anonymously by anyone who has its ID. * `RESTRICTED` - Only users with explicit access to the resource can access the run. When a run is accessible anonymously, all of the run's default storages and logs also become accessible anonymously. Required: runId. Optional: generalAccess, isStatusMessageTerminal, statusMessage. Returns the updated RunResponse.",
      "method": "PUT",
      "path": "/v2/actor-runs/{runId}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "isStatusMessageTerminal",
          "type": "bool",
          "location": "body",
          "description": "Is status message terminal"
        },
        {
          "name": "statusMessage",
          "type": "string",
          "location": "body",
          "description": "Status message"
        }
      ]
    },
    {
      "name": "actor-runs_abort_actor-run-post",
      "description": "Aborts an Actor run and returns an object that contains all the details about the run. Only runs that are starting or running are aborted. For runs with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing. Required: runId. Optional: gracefully.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/abort",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "gracefully",
          "type": "bool",
          "location": "query",
          "description": "If true passed, the Actor run will abort gracefully."
        }
      ]
    },
    {
      "name": "actor-runs_charge_post-run",
      "description": "Charge for events in the run of your [pay per event Actor](https://docs.apify.com/platform/actors/running/actors-in-store#pay-per-event). The event you are charging for must be one of the configured events in your Actor. If the Actor is not set up as pay per event, or if the event is not configured, the endpoint will return an error. The endpoint must be called from the Actor run itself, with the same API token that the run was started with. :::info Learn more about pay-per-event pricing For more details about pay-per-event (PPE) pricing, refer to our [PPE documentation](/platform/actors/publishing/monetize/pay-per-event). :::. Required: runId, count, eventName. Returns the new ChargePostRunResponse.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/charge",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "count",
          "type": "int",
          "location": "body",
          "description": "Count",
          "required": true
        },
        {
          "name": "eventName",
          "type": "string",
          "location": "body",
          "description": "Event name",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_dataset_actor-run-delete",
      "description": "Deletes default dataset associated with an Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [ Delete dataset ](/api/v2/dataset-delete) endpoint. Required: runId. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}/dataset",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_dataset_actor-run-get",
      "description": "Returns the default dataset associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultDatasetId` and then using the [Get dataset](/api/v2/dataset-get) endpoint. Required: runId.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/dataset",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_dataset_actor-run-items-get",
      "description": "Returns data stored in the default dataset of the Actor run in the desired format. This endpoint is a shortcut that resolves the run's `defaultDatasetId` and proxies to the [Get dataset items](/api/v2/dataset-items-get) endpoint. Required: runId. Optional: format, clean, offset (plus 21 more).",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/dataset/items",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "actor-runs_dataset_actor-run-items-post",
      "description": "Appends an item or an array of items to the end of the Actor run's default dataset. This endpoint is a shortcut that resolves the run's `defaultDatasetId` and proxies to the [Store items](/api/v2/dataset-items-post) endpoint. Required: runId. Returns the new DatasetActorRunItemsPostResponse.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/dataset/items",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_dataset_actor-run-put",
      "description": "Updates the default dataset associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultDatasetId` and then using the [Put dataset](/api/v2/dataset-put) endpoint. Required: runId. Optional: generalAccess, name. Returns the updated DatasetResponse.",
      "method": "PUT",
      "path": "/v2/actor-runs/{runId}/dataset",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "actor-runs_dataset_actor-run-statistics-get",
      "description": "Returns statistics for the Actor run's default dataset. This endpoint is a shortcut that resolves the run's `defaultDatasetId` and proxies to the [Get dataset statistics](/api/v2/dataset-statistics-get) endpoint. Required: runId.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/dataset/statistics",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-delete",
      "description": "Delete the default key-value store. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Delete store](/api/v2/key-value-store-delete) endpoint. Required: runId. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}/key-value-store",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-get",
      "description": "Gets an object that contains all the details about the default key-value store. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get store](/api/v2/key-value-store-get) endpoint. Required: runId. Returns the KeyValueStoreResponse.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/key-value-store",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-keys-get",
      "description": "Returns a list of keys for the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get list of keys](/api/v2/key-value-store-keys-get) endpoint. Required: runId. Optional: exclusiveStartKey, limit (default: 1000), collection (plus 2 more).",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/key-value-store/keys",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "exclusiveStartKey",
          "type": "string",
          "location": "query",
          "description": "All keys up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned."
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that belong to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that start with a specific prefix."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-put",
      "description": "Updates the default key-value store's name and general resource access level using a value specified by a JSON object passed in the PUT payload. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Update store](/api/v2/key-value-store-put) endpoint. Required: runId. Optional: generalAccess, name. Returns the updated KeyValueStoreResponse.",
      "method": "PUT",
      "path": "/v2/actor-runs/{runId}/key-value-store",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-record-delete",
      "description": "Removes a record specified by a key from the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Delete record](/api/v2/key-value-store-record-delete) endpoint. Required: runId, recordKey. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-record-get",
      "description": "Gets a value stored under a specific key in the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get record](/api/v2/key-value-store-record-get) endpoint. Required: runId, recordKey. Optional: signature, attachment. Returns the RecordResponse.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header,..."
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-record-post",
      "description": "Stores a value under a specific key in the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-post) endpoint. Required: runId, recordKey. Returns the new KeyValueStoreActorRunRecordPostResponse.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-record-put",
      "description": "Stores a value under a specific key in the default key-value store of the Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-put) endpoint. Required: runId, recordKey. Returns the updated KeyValueStoreActorRunRecordPutResponse.",
      "method": "PUT",
      "path": "/v2/actor-runs/{runId}/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_key-value-store_actor-run-records-get",
      "description": "Downloads all records from the default key-value store of the Actor run as a ZIP archive. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Download records](/api/v2/key-value-store-records-get) endpoint. Required: runId. Optional: collection, prefix, signature.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/key-value-store/records",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "If specified, only records belonging to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "If specified, only records whose key starts with the given prefix are included in the archive."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "actor-runs_log_actor-run-get",
      "description": "Retrieves Actor run's logs. This endpoint is a shortcut for getting the run's log. Same as [Get log](/api/v2/log-get) endpoint. Required: runId. Optional: stream, download, raw.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/log",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "stream",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
        },
        {
          "name": "download",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
        },
        {
          "name": "raw",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the logs will be kept verbatim."
        }
      ]
    },
    {
      "name": "actor-runs_metamorph_actor-run-post",
      "description": "Transforms an Actor run into a run of another Actor with a new input. This is useful if you want to use another Actor to finish the work of your current Actor run, without the need to create a completely new run and waiting for its finish. For the users of your Actors, the metamorph operation is transparent, they will just see your Actor got the work done. Internally, the system stops the Docker container corresponding to the Actor run and starts a new container using a different Docker image. All the default storages are preserved and the new input is stored under the `INPUT-METAMORPH-1` key in the same default key-value store. For more information, see the [Actor docs](https://docs.apify.com/platform/actors/development/programming-interface/metamorph). Required: runId, targetActorId. Optional: build. Returns the new RunResponse.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/metamorph",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "targetActorId",
          "type": "string",
          "location": "query",
          "description": "ID of a target Actor that the run should be transformed into.",
          "required": true
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Optional build of the target Actor."
        }
      ]
    },
    {
      "name": "actor-runs_reboot_actor-run-post",
      "description": "Reboots an Actor run and returns an object that contains all the details about the rebooted run. Only runs that are running, i.e. runs with status `RUNNING` can be rebooted. The run's container will be restarted, so any data not persisted in the key-value store, dataset, or request queue will be lost. Required: runId.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/reboot",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-delete",
      "description": "Deletes the default request queue associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete request queue](/api/v2/request-queue-delete) endpoint. Required: runId. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}/request-queue",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-get",
      "description": "Returns the default request queue associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get request queue](/api/v2/request-queue-get) endpoint. Required: runId.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/request-queue",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-head-get",
      "description": "Returns the given number of first requests from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get head](/api/v2/request-queue-head-get) endpoint. Required: runId. Optional: limit, clientKey.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/request-queue/head",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-head-lock-post",
      "description": "Returns the given number of first requests from the default request queue of the Actor run and locks them for the given time. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get head and lock](/api/v2/request-queue-head-lock-post) endpoint. Required: runId, lockSecs. Optional: limit, clientKey.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/request-queue/head/lock",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from the queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-put",
      "description": "Updates the default request queue associated with an Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Update request queue](/api/v2/request-queue-put) endpoint. Required: runId. Optional: generalAccess, name. Returns the updated RequestQueueResponse.",
      "method": "PUT",
      "path": "/v2/actor-runs/{runId}/request-queue",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "The new name for the request queue."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-request-delete",
      "description": "Deletes a request from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete request](/api/v2/request-queue-request-delete) endpoint. Required: runId, requestId. Optional: clientKey. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-request-get",
      "description": "Returns a request from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Get request](/api/v2/request-queue-request-get) endpoint. Required: runId, requestId.",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-request-lock-delete",
      "description": "Deletes a request lock in the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete request lock](/api/v2/request-queue-request-lock-delete) endpoint. Required: runId, requestId. Optional: clientKey, forefront. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/{requestId}/lock",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock was..."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-request-lock-put",
      "description": "Prolongs a request lock in the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Prolong request lock](/api/v2/request-queue-request-lock-put) endpoint. Required: runId, requestId, lockSecs. Optional: clientKey, forefront. Returns the updated ProlongRequestLockResponse.",
      "method": "PUT",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/{requestId}/lock",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-request-put",
      "description": "Updates a request in the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Update request](/api/v2/request-queue-request-put) endpoint. Required: runId, requestId, id, uniqueKey, url. Optional: forefront, clientKey, errorMessages (plus 8 more). Returns the updated UpdateRequestResponse.",
      "method": "PUT",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "id",
          "type": "string",
          "location": "body",
          "description": "A unique identifier assigned to the request.",
          "required": true
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication.",
          "required": true
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request.",
          "required": true
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-requests-batch-delete",
      "description": "Batch-deletes requests from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Delete requests](/api/v2/request-queue-requests-batch-delete) endpoint. Required: runId. Optional: clientKey. Returns the BatchDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/batch",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-requests-batch-post",
      "description": "Adds requests to the default request queue of the Actor run in batch. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Add requests](/api/v2/request-queue-requests-batch-post) endpoint. Required: runId. Optional: clientKey, forefront. Returns the new BatchAddResponse.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/batch",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-requests-get",
      "description": "Returns a list of requests from the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [List requests](/api/v2/request-queue-requests-get) endpoint. Required: runId. Optional: clientKey, exclusiveStartId, limit (plus 2 more).",
      "method": "GET",
      "path": "/v2/actor-runs/{runId}/request-queue/requests",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "exclusiveStartId",
          "type": "string",
          "location": "query",
          "description": "All requests up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned. Maximum value is `10000`."
        },
        {
          "name": "cursor",
          "type": "string",
          "location": "query",
          "description": "A cursor string for pagination, returned in the previous response as `nextCursor`."
        },
        {
          "name": "filter",
          "type": "array",
          "location": "query",
          "description": "Filter requests by their state."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-requests-post",
      "description": "Adds a request to the default request queue of the Actor run. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Add request](/api/v2/request-queue-requests-post) endpoint. Required: runId. Optional: clientKey, forefront, errorMessages (plus 10 more). Returns the new AddRequestResponse.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/request-queue/requests",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication."
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request."
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "actor-runs_request-queue_actor-run-requests-unlock-post",
      "description": "Unlocks requests in the default request queue of the Actor run that are currently locked by the client. This endpoint is a shortcut for getting the run's `defaultRequestQueueId` and then using the [Unlock requests](/api/v2/request-queue-requests-unlock-post) endpoint. Required: runId. Optional: clientKey. Returns the new UnlockRequestsResponse.",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/request-queue/requests/unlock",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-runs_resurrect_post-run",
      "description": "Resurrects a finished Actor run and returns an object that contains all the details about the resurrected run. Only finished runs, i.e. runs with status `FINISHED`, `FAILED`, `ABORTED` and `TIMED-OUT` can be resurrected. Run status will be updated to RUNNING and its container will be restarted with the same storages (the same behaviour as when the run gets migrated to the new server). For more information, see the [Actor docs](https://docs.apify.com/platform/actors/running/runs-and-builds#resurrection-of-finished-run). Required: runId. Optional: build, timeout, memory (plus 3 more).",
      "method": "POST",
      "path": "/v2/actor-runs/{runId}/resurrect",
      "params": [
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run is resurrected with the same build it originally used. Specifically, if a run was first started with the `latest` tag, which resolves to version `0.0.3` at the time, a run resurrected without this parameter will continue running with `0.0.3`, even if `latest` already points to a newer build."
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout specified in the run that is being resurrected."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes. The amount of memory can be set to a power of 2 with a minimum of 128. By default, the run uses the memory limit specified in the run that is being resurrected."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the resurrected run will be restarted if it fails. By default, the resurrected run uses the same setting as before."
        }
      ]
    },
    {
      "name": "actor-tasks_delete",
      "description": "Delete the task specified through the `actorTaskId` parameter. Required: actorTaskId. Returns the ActorTasksDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_get",
      "description": "Gets the complete list of tasks that a user has created or used. The response is a list of objects in which each object contains essential information about a single task. The endpoint supports pagination using the `limit` and `offset` parameters, and it does not return more than a 1000 records. By default, the records are sorted by the `createdAt` field in ascending order; therefore you can use pagination to incrementally fetch all tasks while new ones are still being created. To sort the records in descending order, use the `desc=1` parameter. Optional: offset, limit, desc. Returns the ListOfTasksResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        }
      ]
    },
    {
      "name": "actor-tasks_get-actortasks",
      "description": "Get an object that contains all the details about a task. Required: actorTaskId. Returns the ActorTasksGetActortasksResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_post",
      "description": "Create a new task with settings specified by the object passed as JSON in the POST payload. The response is the full task object as returned by the [Get task](#/reference/tasks/task-object/get-task) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). Required: actId. Optional: actorStandby, input, name (plus 2 more). Returns the new TaskResponse.",
      "method": "POST",
      "path": "/v2/actor-tasks",
      "params": [
        {
          "name": "actId",
          "type": "string",
          "location": "body",
          "description": "Act id",
          "required": true
        },
        {
          "name": "actorStandby",
          "type": "string",
          "location": "body",
          "description": "Actor standby"
        },
        {
          "name": "input",
          "type": "string",
          "location": "body",
          "description": "Input"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        },
        {
          "name": "options",
          "type": "string",
          "location": "body",
          "description": "Options"
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "Title"
        }
      ]
    },
    {
      "name": "actor-tasks_put",
      "description": "Update settings of a task using values specified by an object passed as JSON in the POST payload. If the object does not define a specific property, its value is not updated. The response is the full task object as returned by the [Get task](#/reference/tasks/task-object/get-task) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). Required: actorTaskId. Optional: actorStandby, input, name (plus 2 more). Returns the updated ActorTasksPutResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "actorStandby",
          "type": "string",
          "location": "body",
          "description": "Actor standby"
        },
        {
          "name": "input",
          "type": "string",
          "location": "body",
          "description": "Input"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        },
        {
          "name": "options",
          "type": "string",
          "location": "body",
          "description": "Options"
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "Title"
        }
      ]
    },
    {
      "name": "actor-tasks_input_actor-task-get",
      "description": "Returns the input of a given task. Required: actorTaskId.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/input",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_input_actor-task-put",
      "description": "Updates the input of a task using values specified by an object passed as JSON in the PUT payload. If the object does not define a specific property, its value is not updated. The response is the full task input as returned by the [Get task input](#/reference/tasks/task-input-object/get-task-input) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). Required: actorTaskId. Returns the updated InputActorTaskPutResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}/input",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_run-sync_actor-task-get",
      "description": "Run a specific task and return its output. The run must finish in 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds otherwise the HTTP request fails with a timeout error (this won't abort the run itself). Beware that it might be impossible to maintain an idle HTTP connection for an extended period, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. To run the Task asynchronously, use the [Run task asynchronously](#/reference/actor-tasks/run-collection/run-task) endpoint instead. Required: actorTaskId. Optional: timeout, memory, maxItems (plus 3 more). Returns the RunSyncActorTaskGetResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/run-sync",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "outputRecordKey",
          "type": "string",
          "location": "query",
          "description": "Key of the record from run's default key-value store to be returned in the response."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        }
      ]
    },
    {
      "name": "actor-tasks_run-sync_actor-task-post",
      "description": "Runs an Actor task and synchronously returns its output. The run must finish in 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds otherwise the HTTP request fails with a timeout error (this won't abort the run itself). Optionally, you can override the Actor input configuration by passing a JSON object as the POST payload and setting the `Content-Type: application/json` HTTP header. Note that if the object in the POST payload does not define a particular input property, the Actor run uses the default value defined by the task (or Actor's input schema if not defined by the task). Beware that it might be impossible to maintain an idle HTTP connection for an extended period, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. Input fields from Actor task configuration can be overloaded with values passed as the POST payload. Just make sure to specify `Content-Type` header to be `application/json` and input to be an object. To run the task asynchronously, use the [Run task](#/reference/actor-tasks/run-collection/run-task) API endpoint instead. Required: actorTaskId. Optional: timeout, memory, maxItems (plus 5 more).",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/run-sync",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "outputRecordKey",
          "type": "string",
          "location": "query",
          "description": "Key of the record from run's default key-value store to be returned in the response."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        }
      ]
    },
    {
      "name": "actor-tasks_run-sync-get-dataset-items_actor-task-get",
      "description": "Run a specific task and return its dataset items. The run must finish in 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds otherwise the HTTP request fails with a timeout error (this won't abort the run itself). You can send all the same options in parameters as the [Get Dataset Items](#/reference/datasets/item-collection/get-items) API endpoint. Beware that it might be impossible to maintain an idle HTTP connection for an extended period, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. To run the Task asynchronously, use the [Run task asynchronously](#/reference/actor-tasks/run-collection/run-task) endpoint instead. Required: actorTaskId. Optional: timeout, memory, maxItems (plus 25 more). Returns the RunSyncGetDatasetItemsActorTaskGetResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/run-sync-get-dataset-items",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        }
      ]
    },
    {
      "name": "actor-tasks_run-sync-get-dataset-items_actor-task-post",
      "description": "Runs an Actor task and synchronously returns its dataset items. The run must finish in 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds otherwise the HTTP request fails with a timeout error (this won't abort the run itself). Optionally, you can override the Actor input configuration by passing a JSON object as the POST payload and setting the `Content-Type: application/json` HTTP header. Note that if the object in the POST payload does not define a particular input property, the Actor run uses the default value defined by the task (or the Actor's input schema if not defined by the task). You can send all the same options in parameters as the [Get Dataset Items](#/reference/datasets/item-collection/get-items) API endpoint. Beware that it might be impossible to maintain an idle HTTP connection for an extended period, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. Input fields from Actor task configuration can be overloaded with values passed as the POST payload. Just make sure to specify the `Content-Type` header as `application/json` and that the input is an object. To run the task asynchronously, use the [Run task](#/reference/actor-tasks/run-collection/run-task) API endpoint instead. Required: actorTaskId. Optional: timeout, memory, maxItems (plus 27 more).",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/run-sync-get-dataset-items",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-get",
      "description": "Get a list of runs of a specific task. The response is a list of objects, where each object contains essential information about a single task run. The endpoint supports pagination using the `limit` and `offset` parameters, and it does not return more than a 1000 array elements. By default, the records are sorted by the `startedAt` field in ascending order; therefore you can use pagination to incrementally fetch all records while new ones are still being created. To sort the records in descending order, use the `desc=1` parameter. You can also filter runs by status ([available statuses](https://docs.apify.com/platform/actors/running/runs-and-builds#lifecycle)). Required: actorTaskId. Optional: offset, limit, desc (plus 1 more). Returns the RunsActorTaskGetResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order."
        },
        {
          "name": "status",
          "type": "array",
          "location": "query",
          "description": "Single status or comma-separated list of statuses, see ([available statuses](https://docs."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-abort-post",
      "description": "Aborts the last run of the specified Actor task and returns an object that contains all the details about the run. This endpoint is a shortcut for [Abort run](#/reference/actor-runs/abort-run/abort-run) on the Actor task's last run. Only runs that are starting or running are aborted. For runs with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing. Required: actorTaskId. Optional: status, gracefully.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/abort",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "gracefully",
          "type": "bool",
          "location": "query",
          "description": "If true passed, the Actor run will abort gracefully."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-dataset-delete",
      "description": "Deletes the default dataset associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultDatasetId` and then using the [Delete dataset](/api/v2/dataset-delete) endpoint. Required: actorTaskId. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/dataset",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-dataset-get",
      "description": "Returns the default dataset associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultDatasetId` and then using the [Get dataset](/api/v2/dataset-get) endpoint. Required: actorTaskId. Optional: status.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/dataset",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-dataset-items-get",
      "description": "Returns data stored in the default dataset of the last Actor task run in the desired format. This endpoint is a shortcut that resolves the last task run's `defaultDatasetId` and proxies to the [Get dataset items](/api/v2/dataset-items-get) endpoint. Required: actorTaskId. Optional: status, format, clean (plus 22 more).",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/dataset/items",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-dataset-items-post",
      "description": "Appends an item or an array of items to the end of the last Actor task run's default dataset. This endpoint is a shortcut that resolves the last task run's `defaultDatasetId` and proxies to the [Store items](/api/v2/dataset-items-post) endpoint. Required: actorTaskId. Optional: status. Returns the new RunsActorTaskLastDatasetItemsPostResponse.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/dataset/items",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-dataset-put",
      "description": "Updates the default dataset associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultDatasetId` and then using the [Update dataset](/api/v2/dataset-put) endpoint. Required: actorTaskId. Optional: status, generalAccess, name. Returns the updated DatasetResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/dataset",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-dataset-statistics-get",
      "description": "Returns statistics for the last Actor task run's default dataset. This endpoint is a shortcut that resolves the last task run's `defaultDatasetId` and proxies to the [Get dataset statistics](/api/v2/dataset-statistics-get) endpoint. Required: actorTaskId. Optional: status.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/dataset/statistics",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-get",
      "description": "This is not a single endpoint, but an entire group of endpoints that lets you to retrieve and manage the last run of given actor task or any of its default storages. All the endpoints require an authentication token. The base path represents the last actor task run object is: `/v2/actor-tasks/{actorTaskId}/runs/last{?token,status}` Using the `status` query parameter you can ensure to only get a run with a certain status (e.g. `status=SUCCEEDED`). The output of this endpoint and other query parameters are the same as in the [Run object](/api/v2/actor-run-get) endpoint. ##### Convenience endpoints for last Actor task run * [Dataset](/api/v2/last-actor-task-runs-default-dataset) * [Key-value store](/api/v2/last-actor-task-runs-default-key-value-store) * [Request queue](/api/v2/last-actor-task-runs-default-request-queue) * [Log](/api/v2/last-actor-task-runs-log). Required: actorTaskId. Optional: status, waitForFinish. Returns the RunsActorTaskLastGetResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the run to finish."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-delete",
      "description": "Deletes the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Delete store](/api/v2/key-value-store-delete) endpoint. Required: actorTaskId. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-get",
      "description": "Gets an object that contains all the details about the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Get store](/api/v2/key-value-store-get) endpoint. Required: actorTaskId. Optional: status. Returns the KeyValueStoreResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-keys-get",
      "description": "Returns a list of keys for the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Get list of keys](/api/v2/key-value-store-keys-get) endpoint. Required: actorTaskId. Optional: status, exclusiveStartKey, limit (default: 1000) (plus 3 more).",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/keys",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "exclusiveStartKey",
          "type": "string",
          "location": "query",
          "description": "All keys up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned."
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that belong to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that start with a specific prefix."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-put",
      "description": "Updates the default key-value store associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Update store](/api/v2/key-value-store-put) endpoint. Required: actorTaskId. Optional: status, generalAccess, name. Returns the updated KeyValueStoreResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-record-delete",
      "description": "Removes a record specified by a key from the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Delete record](/api/v2/key-value-store-record-delete) endpoint. Required: actorTaskId, recordKey. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-record-get",
      "description": "Gets a value stored under a specific key in the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Get record](/api/v2/key-value-store-record-get) endpoint. Required: actorTaskId, recordKey. Optional: status, signature, attachment. Returns the RecordResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header,..."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-record-post",
      "description": "Stores a value under a specific key in the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-post) endpoint. Required: actorTaskId, recordKey. Optional: status. Returns the new RunsActorTaskLastKeyValueStoreRecordPostResponse.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-record-put",
      "description": "Stores a value under a specific key in the default key-value store of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-put) endpoint. Required: actorTaskId, recordKey. Optional: status. Returns the updated RunsActorTaskLastKeyValueStoreRecordPutResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-key-value-store-records-get",
      "description": "Downloads all records from the default key-value store of the last Actor task run as a ZIP archive. This endpoint is a shortcut for getting the last task run's `defaultKeyValueStoreId` and then using the [Download records](/api/v2/key-value-store-records-get) endpoint. Required: actorTaskId. Optional: status, collection, prefix (plus 1 more).",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/key-value-store/records",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "If specified, only records belonging to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "If specified, only records whose key starts with the given prefix are included in the archive."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-log-get",
      "description": "Retrieves last Actor task run's logs. This endpoint is a shortcut for getting last Actor task run's log. Same as [Get log](/api/v2/log-get) endpoint. Required: actorTaskId. Optional: stream, download, raw.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/log",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "stream",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
        },
        {
          "name": "download",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
        },
        {
          "name": "raw",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the logs will be kept verbatim."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-metamorph-post",
      "description": "Transforms the last run of the specified Actor task into a run of another Actor with a new input. This endpoint is a shortcut for [Metamorph run](#/reference/actor-runs/metamorph-run/metamorph-run) on the Actor task's last run. Required: actorTaskId, targetActorId. Optional: status, build. Returns the new RunResponse.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/metamorph",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "targetActorId",
          "type": "string",
          "location": "query",
          "description": "ID of a target Actor that the run should be transformed into.",
          "required": true
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Optional build of the target Actor."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-reboot-post",
      "description": "Reboots the last run of the specified Actor task and returns an object that contains all the details about the rebooted run. This endpoint is a shortcut for [Reboot run](#/reference/actor-runs/reboot-run/reboot-run) on the Actor task's last run. Only runs with status `RUNNING` can be rebooted. The run's container will be restarted, so any data not persisted in the key-value store, dataset, or request queue will be lost. Required: actorTaskId. Optional: status.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/reboot",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-delete",
      "description": "Deletes the default request queue associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete request queue](/api/v2/request-queue-delete) endpoint. Required: actorTaskId. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-get",
      "description": "Returns the default request queue associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get request queue](/api/v2/request-queue-get) endpoint. Required: actorTaskId. Optional: status.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-head-get",
      "description": "Returns the given number of first requests from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get head](/api/v2/request-queue-head-get) endpoint. Required: actorTaskId. Optional: status, limit, clientKey.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/head",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-head-lock-post",
      "description": "Returns the given number of first requests from the default request queue of the last Actor task run and locks them for the given time. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get head and lock](/api/v2/request-queue-head-lock-post) endpoint. Required: actorTaskId, lockSecs. Optional: status, limit, clientKey.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/head/lock",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from the queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-put",
      "description": "Updates the default request queue associated with the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Update request queue](/api/v2/request-queue-put) endpoint. Required: actorTaskId. Optional: status, generalAccess, name. Returns the updated RequestQueueResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "The new name for the request queue."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-request-delete",
      "description": "Deletes a request from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete request](/api/v2/request-queue-request-delete) endpoint. Required: actorTaskId, requestId. Optional: status, clientKey. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-request-get",
      "description": "Returns a request from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Get request](/api/v2/request-queue-request-get) endpoint. Required: actorTaskId, requestId. Optional: status.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-request-lock-delete",
      "description": "Deletes a request lock in the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete request lock](/api/v2/request-queue-request-lock-delete) endpoint. Required: actorTaskId, requestId. Optional: status, clientKey, forefront. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}/lock",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock was..."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-request-lock-put",
      "description": "Prolongs a request lock in the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Prolong request lock](/api/v2/request-queue-request-lock-put) endpoint. Required: actorTaskId, requestId, lockSecs. Optional: status, clientKey, forefront. Returns the updated ProlongRequestLockResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}/lock",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-request-put",
      "description": "Updates a request in the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Update request](/api/v2/request-queue-request-put) endpoint. Required: actorTaskId, requestId, id, uniqueKey, url. Optional: status, forefront, clientKey (plus 9 more). Returns the updated UpdateRequestResponse.",
      "method": "PUT",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "id",
          "type": "string",
          "location": "body",
          "description": "A unique identifier assigned to the request.",
          "required": true
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication.",
          "required": true
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request.",
          "required": true
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-requests-batch-delete",
      "description": "Batch-deletes requests from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Delete requests](/api/v2/request-queue-requests-batch-delete) endpoint. Required: actorTaskId. Optional: status, clientKey. Returns the BatchDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/batch",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-requests-batch-post",
      "description": "Adds requests to the default request queue of the last Actor task run in batch. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Add requests](/api/v2/request-queue-requests-batch-post) endpoint. Required: actorTaskId. Optional: status, clientKey, forefront. Returns the new BatchAddResponse.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/batch",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-requests-get",
      "description": "Returns a list of requests from the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [List requests](/api/v2/request-queue-requests-get) endpoint. Required: actorTaskId. Optional: status, clientKey, exclusiveStartId (plus 3 more).",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "exclusiveStartId",
          "type": "string",
          "location": "query",
          "description": "All requests up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned. Maximum value is `10000`."
        },
        {
          "name": "cursor",
          "type": "string",
          "location": "query",
          "description": "A cursor string for pagination, returned in the previous response as `nextCursor`."
        },
        {
          "name": "filter",
          "type": "array",
          "location": "query",
          "description": "Filter requests by their state."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-requests-post",
      "description": "Adds a request to the default request queue of the last Actor task run. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Add request](/api/v2/request-queue-requests-post) endpoint. Required: actorTaskId. Optional: status, clientKey, forefront (plus 11 more). Returns the new AddRequestResponse.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication."
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request."
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-last-request-queue-requests-unlock-post",
      "description": "Unlocks requests in the default request queue of the last Actor task run that are currently locked by the client. This endpoint is a shortcut for getting the last task run's `defaultRequestQueueId` and then using the [Unlock requests](/api/v2/request-queue-requests-unlock-post) endpoint. Required: actorTaskId. Optional: status, clientKey. Returns the new UnlockRequestsResponse.",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs/last/request-queue/requests/unlock",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "actor-tasks_runs_actor-task-post",
      "description": "Runs an Actor task and immediately returns without waiting for the run to finish. Optionally, you can override the Actor input configuration by passing a JSON object as the POST payload and setting the `Content-Type: application/json` HTTP header. Note that if the object in the POST payload does not define a particular input property, the Actor run uses the default value defined by the task (or Actor's input schema if not defined by the task). The response is the Actor Run object as returned by the [Get run](#/reference/actor-runs/run-object-and-its-storages/get-run) endpoint. If you want to wait for the run to finish and receive the actual output of the Actor run as the response, use one of the [Run task synchronously](#/reference/actor-tasks/run-task-synchronously) API endpoints instead. To fetch the Actor run results that are typically stored in the default dataset, you'll need to pass the ID received in the `defaultDatasetId` field received in the response JSON to the [Get dataset items](#/reference/datasets/item-collection/get-items) API endpoint. Required: actorTaskId. Optional: timeout, memory, maxItems (plus 5 more).",
      "method": "POST",
      "path": "/v2/actor-tasks/{actorTaskId}/runs",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the run to finish."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        }
      ]
    },
    {
      "name": "actor-tasks_webhooks_actor-task-get",
      "description": "Gets the list of webhooks of a specific Actor task. The response is a JSON with the list of objects, where each object contains basic information about a single webhook. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order, to sort the records in descending order, use the `desc=1` parameter. Required: actorTaskId. Optional: offset, limit, desc. Returns the WebhooksActorTaskGetResponse.",
      "method": "GET",
      "path": "/v2/actor-tasks/{actorTaskId}/webhooks",
      "params": [
        {
          "name": "actorTaskId",
          "type": "string",
          "location": "path",
          "description": "Task ID or a tilde-separated owner's username and task's name.",
          "required": true
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        }
      ]
    },
    {
      "name": "acts_delete",
      "description": "Deletes an Actor. Required: actorId. Returns the ActsDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_get",
      "description": "Gets the list of all Actors that the user created or used. The response is a list of objects, where each object contains a basic information about a single Actor. To only get Actors created by the user, add the `my=1` query parameter. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order, therefore you can use pagination to incrementally fetch all Actors while new ones are still being created. To sort the records in descending order, use the `desc=1` parameter. You can also sort by your last run by using the `sortBy=stats.lastRunStartedAt` query parameter. In this case, descending order means the most recently run Actor appears first. Optional: my, offset, limit (plus 2 more). Returns the ListOfActorsResponse.",
      "method": "GET",
      "path": "/v2/acts",
      "params": [
        {
          "name": "my",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the returned list only contains Actors owned by the user. The default value is `false`."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        },
        {
          "name": "sortBy",
          "type": "string",
          "location": "query",
          "description": "Field to sort the records by. The default is `createdAt`. You can also use `stats.lastRunStartedAt` to sort by the most recently ran Actors."
        }
      ]
    },
    {
      "name": "acts_get-actorid",
      "description": "Gets an object that contains all the details about a specific Actor. Required: actorId. Returns the ActorResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_post",
      "description": "Creates a new Actor with settings specified in an Actor object passed as JSON in the POST payload. The response is the full Actor object as returned by the [Get Actor](#/reference/actors/actor-object/get-actor) endpoint. The HTTP request must have the `Content-Type: application/json` HTTP header! The Actor needs to define at least one version of the source code. For more information, see [Version object](#/reference/actors/version-object). If you want to make your Actor [public](https://docs.apify.com/platform/actors/publishing) using `isPublic: true`, you will need to provide the Actor's `title` and the `categories` under which that Actor will be classified in Apify Store. For this, it's best to use the [constants from our `apify-shared-js` package](https://github.com/apify/apify-shared-js/blob/2d43ebc41ece9ad31cd6525bd523fb86939bf860/packages/consts/src/consts.ts#L452-L471). Optional: actorStandby, categories, defaultRunOptions (plus 11 more). Returns the new ActorResponse.",
      "method": "POST",
      "path": "/v2/acts",
      "params": [
        {
          "name": "actorStandby",
          "type": "string",
          "location": "body",
          "description": "Actor standby"
        },
        {
          "name": "categories",
          "type": "array",
          "location": "body",
          "description": "Categories"
        },
        {
          "name": "defaultRunOptions",
          "type": "object",
          "location": "body",
          "description": "Default run options"
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "Description"
        },
        {
          "name": "exampleRunInput",
          "type": "string",
          "location": "body",
          "description": "Example run input"
        },
        {
          "name": "isDeprecated",
          "type": "bool",
          "location": "body",
          "description": "Is deprecated"
        },
        {
          "name": "isPublic",
          "type": "bool",
          "location": "body",
          "description": "Is public"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        },
        {
          "name": "pricingInfos",
          "type": "array",
          "location": "body",
          "description": "Pricing infos"
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "body",
          "description": "Restart on error"
        },
        {
          "name": "seoDescription",
          "type": "string",
          "location": "body",
          "description": "Seo description"
        },
        {
          "name": "seoTitle",
          "type": "string",
          "location": "body",
          "description": "Seo title"
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "Title"
        },
        {
          "name": "versions",
          "type": "array",
          "location": "body",
          "description": "Versions"
        }
      ]
    },
    {
      "name": "acts_put",
      "description": "Updates settings of an Actor using values specified by an Actor object passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The response is the full Actor object as returned by the [Get Actor](#/reference/actors/actor-object/get-actor) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). If you want to make your Actor [public](https://docs.apify.com/platform/actors/publishing) using `isPublic: true`, you will need to provide the Actor's `title` and the `categories` under which that Actor will be classified in Apify Store. For this, it's best to use the [constants from our `apify-shared-js` package](https://github.com/apify/apify-shared-js/blob/2d43ebc41ece9ad31cd6525bd523fb86939bf860/packages/consts/src/consts.ts#L452-L471). Required: actorId. Optional: actorPermissionLevel, actorStandby, categories (plus 13 more). Returns the updated ActorResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "actorPermissionLevel",
          "type": "string",
          "location": "body",
          "description": "Actor permission level"
        },
        {
          "name": "actorStandby",
          "type": "string",
          "location": "body",
          "description": "Actor standby"
        },
        {
          "name": "categories",
          "type": "array",
          "location": "body",
          "description": "Categories"
        },
        {
          "name": "defaultRunOptions",
          "type": "string",
          "location": "body",
          "description": "Default run options"
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "Description"
        },
        {
          "name": "exampleRunInput",
          "type": "string",
          "location": "body",
          "description": "Example run input"
        },
        {
          "name": "isDeprecated",
          "type": "bool",
          "location": "body",
          "description": "Is deprecated"
        },
        {
          "name": "isPublic",
          "type": "bool",
          "location": "body",
          "description": "Is public"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        },
        {
          "name": "pricingInfos",
          "type": "array",
          "location": "body",
          "description": "Pricing infos"
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "body",
          "description": "Restart on error"
        },
        {
          "name": "seoDescription",
          "type": "string",
          "location": "body",
          "description": "Seo description"
        },
        {
          "name": "seoTitle",
          "type": "string",
          "location": "body",
          "description": "Seo title"
        },
        {
          "name": "taggedBuilds",
          "type": "object",
          "location": "body",
          "description": "An object to modify tags on the Actor's builds. The key is the tag name (e.g., _latest_), and the value is either an object with a `buildId` or `null`. This operation is a patch; any existing tags that you omit from this object will be preserved. - **To create or reassign a tag**, provide the tag name with a `buildId`. e.g., to assign the _latest_ tag: \u0026nbsp; ```json { 'latest': { 'buildId': 'z2EryhbfhgSyqj6Hn' } } ``` - **To remove a tag**, provide the tag name with a `null` value. e.g., to remove the _beta_ tag: \u0026nbsp; ```json { 'beta': null } ``` - **To perform multiple operations**, combine them. The following reassigns _latest_ and removes _beta_, while preserving any other existing tags. \u0026nbsp; ```json { 'latest': { 'buildId': 'z2EryhbfhgSyqj6Hn' }, 'beta': null } ```"
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "Title"
        },
        {
          "name": "versions",
          "type": "array",
          "location": "body",
          "description": "Versions"
        }
      ]
    },
    {
      "name": "acts_builds_act-abort-post",
      "description": "**[DEPRECATED]** API endpoints related to build of the Actor were moved under new namespace [`actor-builds`](#/reference/actor-builds). Aborts an Actor build and returns an object that contains all the details about the build. Only builds that are starting or running are aborted. For builds with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing. Required: actorId, buildId.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/builds/{buildId}/abort",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_builds_act-default-get",
      "description": "Get the default build for an Actor. Use the optional `waitForFinish` parameter to synchronously wait for the build to finish. This avoids the need for periodic polling when waiting for the build to complete. This endpoint does not require an authentication token. Instead, calls are authenticated using the Actor's unique ID. However, if you access the endpoint without a token, certain attributes (e.g., `usageUsd` and `usageTotalUsd`) will be hidden. Required: actorId. Optional: waitForFinish. Returns the BuildResponse. (public)",
      "method": "GET",
      "path": "/v2/acts/{actorId}/builds/default",
      "no_auth": true,
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the build to finish."
        }
      ]
    },
    {
      "name": "acts_builds_act-get",
      "description": "Gets the list of builds of a specific Actor. The response is a JSON with the list of objects, where each object contains basic information about a single build. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `startedAt` field in ascending order, therefore you can use pagination to incrementally fetch all builds while new ones are still being started. To sort the records in descending order, use the `desc=1` parameter. Required: actorId. Optional: offset, limit, desc. Returns the ListOfBuildsResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/builds",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order."
        }
      ]
    },
    {
      "name": "acts_builds_act-get-acts",
      "description": "By passing the optional `waitForFinish` parameter the API endpoint will synchronously wait for the build to finish. This is useful to avoid periodic polling when waiting for an Actor build to finish. This endpoint does not require the authentication token. Instead, calls are authenticated using a hard-to-guess ID of the build. However, if you access the endpoint without the token, certain attributes, such as `usageUsd` and `usageTotalUsd`, will be hidden. Required: actorId, buildId. Optional: waitForFinish. Returns the BuildResponse. (public)",
      "method": "GET",
      "path": "/v2/acts/{actorId}/builds/{buildId}",
      "no_auth": true,
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab.",
          "required": true
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the build to finish."
        }
      ]
    },
    {
      "name": "acts_builds_act-openapi-json-get",
      "description": "Get the OpenAPI definition for Actor builds. Two similar endpoints are available: - [First endpoint](/api/v2/act-openapi-json-get): Requires both `actorId` and `buildId`. Use `default` as the `buildId` to get the OpenAPI schema for the default Actor build. - [Second endpoint](/api/v2/actor-build-openapi-json-get): Requires only `buildId`. Get the OpenAPI definition for a specific Actor build. To fetch the default Actor build, simply pass `default` as the `buildId`. Authentication is based on the build's unique ID. No authentication token is required. :::note You can also use the [`/api/v2/actor-build-openapi-json-get`](/api/v2/actor-build-openapi-json-get) endpoint to get the OpenAPI definition for a build. :::. Required: actorId, buildId. Returns the BuildsActOpenapiJsonGetResponse. (public)",
      "method": "GET",
      "path": "/v2/acts/{actorId}/builds/{buildId}/openapi.json",
      "no_auth": true,
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "buildId",
          "type": "string",
          "location": "path",
          "description": "ID of the build, found in the build's Info tab. Use the special value `default` to get the OpenAPI schema for the Actor's default build.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_builds_act-post",
      "description": "Builds an Actor. The response is the build object as returned by the [Get build](#/reference/actors/build-object/get-build) endpoint. Required: actorId, version. Optional: useCache, betaPackages, tag (plus 1 more). Returns the new BuildResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/builds",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "version",
          "type": "string",
          "location": "query",
          "description": "Actor version number to be built.",
          "required": true
        },
        {
          "name": "useCache",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the system will use a cache to speed up the build process. By default, cache is not used."
        },
        {
          "name": "betaPackages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the Actor is built with beta versions of Apify NPM packages. By default, the build uses `latest` packages."
        },
        {
          "name": "tag",
          "type": "string",
          "location": "query",
          "description": "Tag to be applied to the build on success. By default, the tag is taken from Actor version's `buildTag` property."
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the build to finish."
        }
      ]
    },
    {
      "name": "acts_run-sync_act-get",
      "description": "Runs a specific Actor and returns its output. The run must finish in 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds otherwise the API endpoint returns a timeout error. The Actor is not passed any input. Beware that it might be impossible to maintain an idle HTTP connection for a long period of time, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. To run the Actor asynchronously, use the [Run Actor](#/reference/actors/run-collection/run-actor) API endpoint instead. Required: actorId. Optional: outputRecordKey, timeout, memory (plus 5 more).",
      "method": "GET",
      "path": "/v2/acts/{actorId}/run-sync",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "outputRecordKey",
          "type": "string",
          "location": "query",
          "description": "Key of the record from run's default key-value store to be returned in the response."
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        }
      ]
    },
    {
      "name": "acts_run-sync_act-post",
      "description": "Runs a specific Actor and returns its output. The POST payload including its `Content-Type` header is passed as `INPUT` to the Actor (usually \u003ccode\u003eapplication/json\u003c/code\u003e). The HTTP response contains Actors `OUTPUT` record from its default key-value store. The Actor is started with the default options; you can override them using various URL query parameters. If the Actor run exceeds 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds, the HTTP response will have status 408 (Request Timeout). Beware that it might be impossible to maintain an idle HTTP connection for a long period of time, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. To run the Actor asynchronously, use the [Run Actor](#/reference/actors/run-collection/run-actor) API endpoint instead. Required: actorId. Optional: outputRecordKey, timeout, memory (plus 5 more).",
      "method": "POST",
      "path": "/v2/acts/{actorId}/run-sync",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "outputRecordKey",
          "type": "string",
          "location": "query",
          "description": "Key of the record from run's default key-value store to be returned in the response."
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        }
      ]
    },
    {
      "name": "acts_run-sync-get-dataset-items_act-get",
      "description": "Runs a specific Actor and returns its dataset items. The run must finish in 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds otherwise the API endpoint returns a timeout error. The Actor is not passed any input. It allows to send all possible options in parameters from [Get Dataset Items](#/reference/datasets/item-collection/get-items) API endpoint. Beware that it might be impossible to maintain an idle HTTP connection for a long period of time, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. To run the Actor asynchronously, use the [Run Actor](#/reference/actors/run-collection/run-actor) API endpoint instead. Required: actorId. Optional: timeout, memory, maxItems (plus 27 more).",
      "method": "GET",
      "path": "/v2/acts/{actorId}/run-sync-get-dataset-items",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        }
      ]
    },
    {
      "name": "acts_run-sync-get-dataset-items_act-post",
      "description": "Runs a specific Actor and returns its dataset items. The POST payload including its `Content-Type` header is passed as `INPUT` to the Actor (usually `application/json`). The HTTP response contains the Actors dataset items, while the format of items depends on specifying dataset items' `format` parameter. You can send all the same options in parameters as the [Get Dataset Items](#/reference/datasets/item-collection/get-items) API endpoint. The Actor is started with the default options; you can override them using URL query parameters. If the Actor run exceeds 300\u003c!-- MAX_ACTOR_JOB_SYNC_WAIT_SECS --\u003e seconds, the HTTP response will return the 408 status code (Request Timeout). Beware that it might be impossible to maintain an idle HTTP connection for a long period of time, due to client timeout or network conditions. Make sure your HTTP client is configured to have a long enough connection timeout. If the connection breaks, you will not receive any information about the run and its status. To run the Actor asynchronously, use the [Run Actor](#/reference/actors/run-collection/run-actor) API endpoint instead. Required: actorId. Optional: timeout, memory, maxItems (plus 27 more).",
      "method": "POST",
      "path": "/v2/acts/{actorId}/run-sync-get-dataset-items",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        }
      ]
    },
    {
      "name": "acts_runs_act-abort-post",
      "description": "**[DEPRECATED]** API endpoints related to run of the Actor were moved under new namespace [`actor-runs`](#/reference/actor-runs). Aborts an Actor run and returns an object that contains all the details about the run. Only runs that are starting or running are aborted. For runs with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing. Required: actorId, runId. Optional: gracefully.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/{runId}/abort",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "gracefully",
          "type": "bool",
          "location": "query",
          "description": "If true passed, the Actor run will abort gracefully."
        }
      ]
    },
    {
      "name": "acts_runs_act-get",
      "description": "Gets the list of runs of a specific Actor. The response is a list of objects, where each object contains basic information about a single Actor run. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 array elements. By default, the records are sorted by the `startedAt` field in ascending order, therefore you can use pagination to incrementally fetch all records while new ones are still being created. To sort the records in descending order, use `desc=1` parameter. You can also filter runs by status ([available statuses](https://docs.apify.com/platform/actors/running/runs-and-builds#lifecycle)). Required: actorId. Optional: offset, limit, desc (plus 3 more). Returns the ListOfRunsResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `startedAt` field in descending order."
        },
        {
          "name": "status",
          "type": "array",
          "location": "query",
          "description": "Single status or comma-separated list of statuses, see ([available statuses](https://docs."
        },
        {
          "name": "startedAfter",
          "type": "string",
          "location": "query",
          "description": "Filter runs that started after the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
        },
        {
          "name": "startedBefore",
          "type": "string",
          "location": "query",
          "description": "Filter runs that started before the specified date and time (inclusive). The value must be a valid ISO 8601 datetime string (UTC)."
        }
      ]
    },
    {
      "name": "acts_runs_act-get-acts",
      "description": "**[DEPRECATED]** API endpoints related to run of the Actor were moved under new namespace [`actor-runs`](#/reference/actor-runs). Gets an object that contains all the details about a specific run of an Actor. By passing the optional `waitForFinish` parameter the API endpoint will synchronously wait for the run to finish. This is useful to avoid periodic polling when waiting for Actor run to complete. This endpoint does not require the authentication token. Instead, calls are authenticated using a hard-to-guess ID of the run. However, if you access the endpoint without the token, certain attributes, such as `usageUsd` and `usageTotalUsd`, will be hidden. Required: actorId, runId. Optional: waitForFinish. Returns the RunResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/{runId}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the run to finish."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-abort-post",
      "description": "Aborts the last run of the specified Actor and returns an object that contains all the details about the run. This endpoint is a shortcut for [Abort run](#/reference/actor-runs/abort-run/abort-run) on the Actor's last run. Only runs that are starting or running are aborted. For runs with status `FINISHED`, `FAILED`, `ABORTING` and `TIMED-OUT` this call does nothing. Required: actorId. Optional: status, gracefully.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/abort",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "gracefully",
          "type": "bool",
          "location": "query",
          "description": "If true passed, the Actor run will abort gracefully."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-dataset-delete",
      "description": "Deletes the default dataset associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [Delete dataset](/api/v2/dataset-delete) endpoint. Required: actorId. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/runs/last/dataset",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-dataset-get",
      "description": "Returns the default dataset associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [Get dataset](/api/v2/dataset-get) endpoint. Required: actorId. Optional: status.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/dataset",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-dataset-items-get",
      "description": "Returns data stored in the default dataset of the last Actor run in the desired format. This endpoint is a shortcut that resolves the last run's `defaultDatasetId` and proxies to the [Get dataset items](/api/v2/dataset-items-get) endpoint. Required: actorId. Optional: status, format, clean (plus 22 more).",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/dataset/items",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-dataset-items-post",
      "description": "Appends an item or an array of items to the end of the last Actor run's default dataset. This endpoint is a shortcut that resolves the last run's `defaultDatasetId` and proxies to the [Store items](/api/v2/dataset-items-post) endpoint. Required: actorId. Optional: status. Returns the new RunsActLastDatasetItemsPostResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/dataset/items",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-dataset-put",
      "description": "Updates the default dataset associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultDatasetId` and then using the [Update dataset](/api/v2/dataset-put) endpoint. Required: actorId. Optional: status, generalAccess, name. Returns the updated DatasetResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/runs/last/dataset",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "acts_runs_act-last-dataset-statistics-get",
      "description": "Returns statistics for the last Actor run's default dataset. This endpoint is a shortcut that resolves the last run's `defaultDatasetId` and proxies to the [Get dataset statistics](/api/v2/dataset-statistics-get) endpoint. Required: actorId. Optional: status.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/dataset/statistics",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-get",
      "description": "This is not a single endpoint, but an entire group of endpoints that lets you to retrieve and manage the last run of given Actor or any of its default storages. All the endpoints require an authentication token. The base path represents the last Actor run object is: `/v2/acts/{actorId}/runs/last{?token,status}` Using the `status` query parameter you can ensure to only get a run with a certain status (e.g. `status=SUCCEEDED`). The output of this endpoint and other query parameters are the same as in the [Run object](#/reference/actors/run-object) endpoint. ##### Convenience endpoints for last Actor run * [Dataset](/api/v2/last-actor-runs-default-dataset) * [Key-value store](/api/v2/last-actor-runs-default-key-value-store) * [Request queue](/api/v2/last-actor-runs-default-request-queue) * [Log](/api/v2/last-actor-runs-log). Required: actorId. Optional: status, waitForFinish. Returns the RunResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the run to finish."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-delete",
      "description": "Deletes the last Actor run key-value store. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Delete store](/api/v2/key-value-store-delete) endpoint. Required: actorId. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-get",
      "description": "Gets an object that contains all the details about the default key-value store associated with the last Actor run. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Get store](/api/v2/key-value-store-get) endpoint. Required: actorId. Optional: status. Returns the KeyValueStoreResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-keys-get",
      "description": "Returns a list of keys for the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Get list of keys](/api/v2/key-value-store-keys-get) endpoint. Required: actorId. Optional: status, exclusiveStartKey, limit (default: 1000) (plus 3 more).",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store/keys",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "exclusiveStartKey",
          "type": "string",
          "location": "query",
          "description": "All keys up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned."
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that belong to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that start with a specific prefix."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-put",
      "description": "Updates the last Actor run key-value store's name and general resource access level using a value specified by a JSON object passed in the PUT payload. This endpoint is a shortcut for getting the run's `defaultKeyValueStoreId` and then using the [Update store](/api/v2/key-value-store-put) endpoint. Required: actorId. Optional: status, generalAccess, name. Returns the updated KeyValueStoreResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-record-delete",
      "description": "Removes a record specified by a key from the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Delete record](/api/v2/key-value-store-record-delete) endpoint. Required: actorId, recordKey. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-record-get",
      "description": "Gets a value stored under a specific key in the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Get record](/api/v2/key-value-store-record-get) endpoint. Required: actorId, recordKey. Optional: status, signature, attachment. Returns the RecordResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header,..."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-record-post",
      "description": "Stores a value under a specific key in the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-post) endpoint. Required: actorId, recordKey. Optional: status. Returns the new RunsActLastKeyValueStoreRecordPostResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-record-put",
      "description": "Stores a value under a specific key in the default key-value store of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Store record](/api/v2/key-value-store-record-put) endpoint. Required: actorId, recordKey. Optional: status. Returns the updated RunsActLastKeyValueStoreRecordPutResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store/records/{recordKey}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_runs_act-last-key-value-store-records-get",
      "description": "Downloads all records from the default key-value store of the last Actor run as a ZIP archive. This endpoint is a shortcut for getting the last run's `defaultKeyValueStoreId` and then using the [Download records](/api/v2/key-value-store-records-get) endpoint. Required: actorId. Optional: status, collection, prefix (plus 1 more).",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/key-value-store/records",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "If specified, only records belonging to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "If specified, only records whose key starts with the given prefix are included in the archive."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-log-get",
      "description": "Retrieves last Actor run's logs. This endpoint is a shortcut for getting last Actor run's log. Same as [Get log](/api/v2/log-get) endpoint. Required: actorId. Optional: stream, download, raw.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/log",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "stream",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
        },
        {
          "name": "download",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
        },
        {
          "name": "raw",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the logs will be kept verbatim."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-metamorph-post",
      "description": "Transforms the last run of the specified Actor into a run of another Actor with a new input. This endpoint is a shortcut for [Metamorph run](#/reference/actor-runs/metamorph-run/metamorph-run) on the Actor's last run. Required: actorId, targetActorId. Optional: status, build. Returns the new RunResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/metamorph",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "targetActorId",
          "type": "string",
          "location": "query",
          "description": "ID of a target Actor that the run should be transformed into.",
          "required": true
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Optional build of the target Actor."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-reboot-post",
      "description": "Reboots the last run of the specified Actor and returns an object that contains all the details about the rebooted run. This endpoint is a shortcut for [Reboot run](#/reference/actor-runs/reboot-run/reboot-run) on the Actor's last run. Only runs with status `RUNNING` can be rebooted. The run's container will be restarted, so any data not persisted in the key-value store, dataset, or request queue will be lost. Required: actorId. Optional: status.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/reboot",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-delete",
      "description": "Deletes the default request queue associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete request queue](/api/v2/request-queue-delete) endpoint. Required: actorId. Optional: status. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/runs/last/request-queue",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-get",
      "description": "Returns the default request queue associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get request queue](/api/v2/request-queue-get) endpoint. Required: actorId. Optional: status.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/request-queue",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-head-get",
      "description": "Returns the given number of first requests from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get head](/api/v2/request-queue-head-get) endpoint. Required: actorId. Optional: status, limit, clientKey.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/head",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-head-lock-post",
      "description": "Returns the given number of first requests from the default request queue of the last Actor run and locks them for the given time. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get head and lock](/api/v2/request-queue-head-lock-post) endpoint. Required: actorId, lockSecs. Optional: status, limit, clientKey.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/head/lock",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from the queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-put",
      "description": "Updates the default request queue associated with the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Update request queue](/api/v2/request-queue-put) endpoint. Required: actorId. Optional: status, generalAccess, name. Returns the updated RequestQueueResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/runs/last/request-queue",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "The new name for the request queue."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-request-delete",
      "description": "Deletes a request from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete request](/api/v2/request-queue-request-delete) endpoint. Required: actorId, requestId. Optional: status, clientKey. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-request-get",
      "description": "Returns a request from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Get request](/api/v2/request-queue-request-get) endpoint. Required: actorId, requestId. Optional: status.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-request-lock-delete",
      "description": "Deletes a request lock in the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete request lock](/api/v2/request-queue-request-lock-delete) endpoint. Required: actorId, requestId. Optional: status, clientKey, forefront. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/{requestId}/lock",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock was..."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-request-lock-put",
      "description": "Prolongs a request lock in the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Prolong request lock](/api/v2/request-queue-request-lock-put) endpoint. Required: actorId, requestId, lockSecs. Optional: status, clientKey, forefront. Returns the updated ProlongRequestLockResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/{requestId}/lock",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-request-put",
      "description": "Updates a request in the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Update request](/api/v2/request-queue-request-put) endpoint. Required: actorId, requestId, id, uniqueKey, url. Optional: status, forefront, clientKey (plus 9 more). Returns the updated UpdateRequestResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/{requestId}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "id",
          "type": "string",
          "location": "body",
          "description": "A unique identifier assigned to the request.",
          "required": true
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication.",
          "required": true
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request.",
          "required": true
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-requests-batch-delete",
      "description": "Batch-deletes requests from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Delete requests](/api/v2/request-queue-requests-batch-delete) endpoint. Required: actorId. Optional: status, clientKey. Returns the BatchDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/batch",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-requests-batch-post",
      "description": "Adds requests to the default request queue of the last Actor run in batch. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Add requests](/api/v2/request-queue-requests-batch-post) endpoint. Required: actorId. Optional: status, clientKey, forefront. Returns the new BatchAddResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/batch",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-requests-get",
      "description": "Returns a list of requests from the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [List requests](/api/v2/request-queue-requests-get) endpoint. Required: actorId. Optional: status, clientKey, exclusiveStartId (plus 3 more).",
      "method": "GET",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "exclusiveStartId",
          "type": "string",
          "location": "query",
          "description": "All requests up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned. Maximum value is `10000`."
        },
        {
          "name": "cursor",
          "type": "string",
          "location": "query",
          "description": "A cursor string for pagination, returned in the previous response as `nextCursor`."
        },
        {
          "name": "filter",
          "type": "array",
          "location": "query",
          "description": "Filter requests by their state."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-requests-post",
      "description": "Adds a request to the default request queue of the last Actor run. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Add request](/api/v2/request-queue-requests-post) endpoint. Required: actorId. Optional: status, clientKey, forefront (plus 11 more). Returns the new AddRequestResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication."
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request."
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "acts_runs_act-last-request-queue-requests-unlock-post",
      "description": "Unlocks requests in the default request queue of the last Actor run that are currently locked by the client. This endpoint is a shortcut for getting the last run's `defaultRequestQueueId` and then using the [Unlock requests](/api/v2/request-queue-requests-unlock-post) endpoint. Required: actorId. Optional: status, clientKey. Returns the new UnlockRequestsResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/last/request-queue/requests/unlock",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "location": "query",
          "description": "Filter for the run status."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "acts_runs_act-metamorph-post",
      "description": "**[DEPRECATED]** API endpoints related to run of the Actor were moved under new namespace [`actor-runs`](#/reference/actor-runs). Transforms an Actor run into a run of another Actor with a new input. This is useful if you want to use another Actor to finish the work of your current Actor run, without the need to create a completely new run and waiting for its finish. For the users of your Actors, the metamorph operation is transparent, they will just see your Actor got the work done. There is a limit on how many times you can metamorph a single run. You can check the limit in [the Actor runtime limits](https://docs.apify.com/platform/limits#actor-limits). Internally, the system stops the Docker container corresponding to the Actor run and starts a new container using a different Docker image. All the default storages are preserved and the new input is stored under the `INPUT-METAMORPH-1` key in the same default key-value store. For more information, see the [Actor docs](https://docs.apify.com/platform/actors/development/programming-interface/metamorph). Required: actorId, runId, targetActorId. Optional: build. Returns the new RunResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/{runId}/metamorph",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "targetActorId",
          "type": "string",
          "location": "query",
          "description": "ID of a target Actor that the run should be transformed into.",
          "required": true
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Optional build of the target Actor."
        }
      ]
    },
    {
      "name": "acts_runs_act-post",
      "description": "Runs an Actor and immediately returns without waiting for the run to finish. The POST payload including its `Content-Type` header is passed as `INPUT` to the Actor (usually `application/json`). The Actor is started with the default options; you can override them using various URL query parameters. The response is the Run object as returned by the [Get run](#/reference/actor-runs/run-object-and-its-storages/get-run) API endpoint. If you want to wait for the run to finish and receive the actual output of the Actor as the response, please use one of the [Run Actor synchronously](#/reference/actors/run-actor-synchronously) API endpoints instead. To fetch the Actor run results that are typically stored in the default dataset, you'll need to pass the ID received in the `defaultDatasetId` field received in the response JSON to the [Get dataset items](#/reference/datasets/item-collection/get-items) API endpoint. Required: actorId. Optional: timeout, memory, maxItems (plus 6 more).",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes."
        },
        {
          "name": "maxItems",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum number of dataset items that will be charged for pay-per-result Actors."
        },
        {
          "name": "maxTotalChargeUsd",
          "type": "float",
          "location": "query",
          "description": "Specifies the maximum cost of the run."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the run will be restarted if it fails."
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run."
        },
        {
          "name": "waitForFinish",
          "type": "float",
          "location": "query",
          "description": "The maximum number of seconds the server waits for the run to finish."
        },
        {
          "name": "webhooks",
          "type": "string",
          "location": "query",
          "description": "Specifies optional webhooks associated with the Actor run, which can be used to receive a..."
        },
        {
          "name": "forcePermissionLevel",
          "type": "string",
          "location": "query",
          "description": "Overrides the Actor's permission level for this specific run. Use to test restricted permissions before deploying changes to your Actor or to temporarily elevate or restrict access. If you don't specify this parameter, the Actor uses its configured default permission level. For more information on permissions, see the [documentation](https://docs.apify.com/platform/actors/development/permissions)."
        }
      ]
    },
    {
      "name": "acts_runs_act-resurrect-post",
      "description": "**[DEPRECATED]** API endpoints related to run of the Actor were moved under new namespace [`actor-runs`](#/reference/actor-runs).Resurrects a finished Actor run and returns an object that contains all the details about the resurrected run. Only finished runs, i.e. runs with status `FINISHED`, `FAILED`, `ABORTED` and `TIMED-OUT` can be resurrected. Run status will be updated to RUNNING and its container will be restarted with the same storages (the same behaviour as when the run gets migrated to the new server). For more information, see the [Actor docs](https://docs.apify.com/platform/actors/running/runs-and-builds#resurrection-of-finished-run). Required: actorId, runId. Optional: build, timeout, memory (plus 1 more).",
      "method": "POST",
      "path": "/v2/acts/{actorId}/runs/{runId}/resurrect",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "runId",
          "type": "string",
          "location": "path",
          "description": "Actor run ID.",
          "required": true
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Specifies the Actor build to run. It can be either a build tag or build number. By default, the run is resurrected with the same build it originally used. Specifically, if a run was first started with the `latest` tag, which resolves to version `0.0.3` at the time, a run resurrected without this parameter will continue running with `0.0.3`, even if `latest` already points to a newer build."
        },
        {
          "name": "timeout",
          "type": "float",
          "location": "query",
          "description": "Optional timeout for the run, in seconds. By default, the run uses the timeout specified in the run that is being resurrected."
        },
        {
          "name": "memory",
          "type": "float",
          "location": "query",
          "description": "Memory limit for the run, in megabytes. The amount of memory can be set to a power of 2 with a minimum of 128. By default, the run uses the memory limit specified in the run that is being resurrected."
        },
        {
          "name": "restartOnError",
          "type": "bool",
          "location": "query",
          "description": "Determines whether the resurrected run will be restarted if it fails. By default, the resurrected run uses the same setting as before."
        }
      ]
    },
    {
      "name": "acts_validate-input_act-post",
      "description": "Validates the provided input against the Actor's input schema for the specified build. The endpoint checks whether the JSON payload conforms to the input schema defined in the Actor's build. If no `build` query parameter is provided, the `latest` build tag is used by default. Required: actorId. Optional: build. Returns the new ValidateInputActPostResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/validate-input",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "build",
          "type": "string",
          "location": "query",
          "description": "Optional tag or number of the Actor build to use for input schema validation. By default, the `latest` build tag is used."
        }
      ]
    },
    {
      "name": "acts_versions_act-delete",
      "description": "Deletes a specific version of Actor's source code. Required: actorId, versionNumber. Returns the VersionsActDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-env-var-delete",
      "description": "Deletes a specific environment variable. Required: actorId, versionNumber, envVarName. Returns the VersionsActEnvVarDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        },
        {
          "name": "envVarName",
          "type": "string",
          "location": "path",
          "description": "The name of the environment variable",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-env-var-get",
      "description": "Gets a [EnvVar object](#/reference/actors/environment-variable-object) that contains all the details about a specific environment variable of an Actor. If `isSecret` is set to `true`, then `value` will never be returned. Required: actorId, versionNumber, envVarName. Returns the EnvVarResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        },
        {
          "name": "envVarName",
          "type": "string",
          "location": "path",
          "description": "The name of the environment variable",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-env-var-post",
      "description": "Updates Actor environment variable using values specified by a [EnvVar object](#/reference/actors/environment-variable-object) passed as JSON in the POST payload. This endpoint is an alias for the [`PUT` update environment variable](#tag/ActorsEnvironment-variable-object/operation/act_version_envVar_put) method and behaves identically. Required: actorId, versionNumber, envVarName, name, value. Optional: isSecret. Returns the new EnvVarResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        },
        {
          "name": "envVarName",
          "type": "string",
          "location": "path",
          "description": "The name of the environment variable",
          "required": true
        },
        {
          "name": "isSecret",
          "type": "bool",
          "location": "body",
          "description": "Is secret"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name",
          "required": true
        },
        {
          "name": "value",
          "type": "string",
          "location": "body",
          "description": "The environment variable value.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-env-var-put",
      "description": "Updates Actor environment variable using values specified by a [EnvVar object](#/reference/actors/environment-variable-object) passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). The response is the [EnvVar object](#/reference/actors/environment-variable-object) as returned by the [Get environment variable](#/reference/actors/environment-variable-object/get-environment-variable) endpoint. Required: actorId, versionNumber, envVarName, name, value. Optional: isSecret. Returns the updated EnvVarResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}/env-vars/{envVarName}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        },
        {
          "name": "envVarName",
          "type": "string",
          "location": "path",
          "description": "The name of the environment variable",
          "required": true
        },
        {
          "name": "isSecret",
          "type": "bool",
          "location": "body",
          "description": "Is secret"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name",
          "required": true
        },
        {
          "name": "value",
          "type": "string",
          "location": "body",
          "description": "The environment variable value.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-env-vars-get",
      "description": "Gets the list of environment variables for a specific version of an Actor. The response is a JSON object with the list of [EnvVar objects](#/reference/actors/environment-variable-object), where each contains basic information about a single environment variable. Required: actorId, versionNumber. Returns the ListOfEnvVarsResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}/env-vars",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-env-vars-post",
      "description": "Creates an environment variable of an Actor using values specified in a [EnvVar object](#/reference/actors/environment-variable-object) passed as JSON in the POST payload. The request must specify `name` and `value` parameters (as strings) in the JSON payload and a `Content-Type: application/json` HTTP header. ``` { 'name': 'ENV_VAR_NAME', 'value': 'my-env-var' } ``` The response is the [EnvVar object](#/reference/actors/environment-variable-object) as returned by the [Get environment variable](#/reference/actors/environment-variable-object/get-environment-variable) endpoint. Required: actorId, versionNumber, name, value. Optional: isSecret. Returns the new EnvVarResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}/env-vars",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        },
        {
          "name": "isSecret",
          "type": "bool",
          "location": "body",
          "description": "Is secret"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name",
          "required": true
        },
        {
          "name": "value",
          "type": "string",
          "location": "body",
          "description": "The environment variable value.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-get",
      "description": "Gets the list of versions of a specific Actor. The response is a JSON object with the list of [Version objects](#/reference/actors/version-object), where each contains basic information about a single version. Required: actorId. Returns the ListOfVersionsResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/versions",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-get-acts",
      "description": "Gets a [Version object](#/reference/actors/version-object) that contains all the details about a specific version of an Actor. Required: actorId, versionNumber. Returns the VersionResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        }
      ]
    },
    {
      "name": "acts_versions_act-post",
      "description": "Creates a version of an Actor using values specified in a [Version object](#/reference/actors/version-object) passed as JSON in the POST payload. The request must specify `versionNumber` and `sourceType` parameters (as strings) in the JSON payload and a `Content-Type: application/json` HTTP header. Each `sourceType` requires its own additional properties to be passed to the JSON payload object. These are outlined in the [Version object](#/reference/actors/version-object) table below and in more detail in the [Apify documentation](https://docs.apify.com/platform/actors/development/deployment/source-types). For example, if an Actor's source code is stored in a [GitHub repository](https://docs.apify.com/platform/actors/development/deployment/source-types#git-repository), you will set the `sourceType` to `GIT_REPO` and pass the repository's URL in the `gitRepoUrl` property. ``` { 'versionNumber': '0.1', 'sourceType': 'GIT_REPO', 'gitRepoUrl': 'https://github.com/my-github-account/actor-repo' } ``` The response is the [Version object](#/reference/actors/version-object) as returned by the [Get version](#/reference/actors/version-object/get-version) endpoint. Required: actorId. Optional: applyEnvVarsToBuild, buildTag, envVars (plus 6 more). Returns the new VersionResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/versions",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "applyEnvVarsToBuild",
          "type": "bool",
          "location": "body",
          "description": "Apply env vars to build"
        },
        {
          "name": "buildTag",
          "type": "string",
          "location": "body",
          "description": "Build tag"
        },
        {
          "name": "envVars",
          "type": "array",
          "location": "body",
          "description": "Env vars"
        },
        {
          "name": "gitHubGistUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the GitHub Gist when sourceType is GITHUB_GIST."
        },
        {
          "name": "gitRepoUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the Git repository when sourceType is GIT_REPO."
        },
        {
          "name": "sourceFiles",
          "type": "array",
          "location": "body",
          "description": "Source files"
        },
        {
          "name": "sourceType",
          "type": "string",
          "location": "body",
          "description": "Source type"
        },
        {
          "name": "tarballUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the tarball when sourceType is TARBALL."
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "body",
          "description": "Version number"
        }
      ]
    },
    {
      "name": "acts_versions_act-post-acts",
      "description": "Updates Actor version using values specified by a [Version object](#/reference/actors/version-object) passed as JSON in the POST payload. This endpoint is an alias for the [`PUT` update version](#tag/ActorsVersion-object/operation/act_version_put) method and behaves identically. Required: actorId, versionNumber. Optional: applyEnvVarsToBuild, buildTag, envVars (plus 5 more). Returns the new VersionResponse.",
      "method": "POST",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        },
        {
          "name": "applyEnvVarsToBuild",
          "type": "bool",
          "location": "body",
          "description": "Apply env vars to build"
        },
        {
          "name": "buildTag",
          "type": "string",
          "location": "body",
          "description": "Build tag"
        },
        {
          "name": "envVars",
          "type": "array",
          "location": "body",
          "description": "Env vars"
        },
        {
          "name": "gitHubGistUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the GitHub Gist when sourceType is GITHUB_GIST."
        },
        {
          "name": "gitRepoUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the Git repository when sourceType is GIT_REPO."
        },
        {
          "name": "sourceFiles",
          "type": "array",
          "location": "body",
          "description": "Source files"
        },
        {
          "name": "sourceType",
          "type": "string",
          "location": "body",
          "description": "Source type"
        },
        {
          "name": "tarballUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the tarball when sourceType is TARBALL."
        }
      ]
    },
    {
      "name": "acts_versions_act-put",
      "description": "Updates Actor version using values specified by a [Version object](#/reference/actors/version-object) passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). The response is the [Version object](#/reference/actors/version-object) as returned by the [Get version](#/reference/actors/version-object/get-version) endpoint. Required: actorId, versionNumber. Optional: applyEnvVarsToBuild, buildTag, envVars (plus 5 more). Returns the updated VersionResponse.",
      "method": "PUT",
      "path": "/v2/acts/{actorId}/versions/{versionNumber}",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "versionNumber",
          "type": "string",
          "location": "path",
          "description": "Actor version.",
          "required": true
        },
        {
          "name": "applyEnvVarsToBuild",
          "type": "bool",
          "location": "body",
          "description": "Apply env vars to build"
        },
        {
          "name": "buildTag",
          "type": "string",
          "location": "body",
          "description": "Build tag"
        },
        {
          "name": "envVars",
          "type": "array",
          "location": "body",
          "description": "Env vars"
        },
        {
          "name": "gitHubGistUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the GitHub Gist when sourceType is GITHUB_GIST."
        },
        {
          "name": "gitRepoUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the Git repository when sourceType is GIT_REPO."
        },
        {
          "name": "sourceFiles",
          "type": "array",
          "location": "body",
          "description": "Source files"
        },
        {
          "name": "sourceType",
          "type": "string",
          "location": "body",
          "description": "Source type"
        },
        {
          "name": "tarballUrl",
          "type": "string",
          "location": "body",
          "description": "URL of the tarball when sourceType is TARBALL."
        }
      ]
    },
    {
      "name": "acts_webhooks_act-get",
      "description": "Gets the list of webhooks of a specific Actor. The response is a JSON with the list of objects, where each object contains basic information about a single webhook. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order, to sort the records in descending order, use the `desc=1` parameter. Required: actorId. Optional: offset, limit, desc. Returns the ListOfWebhooksResponse.",
      "method": "GET",
      "path": "/v2/acts/{actorId}/webhooks",
      "params": [
        {
          "name": "actorId",
          "type": "string",
          "location": "path",
          "description": "Actor ID or a tilde-separated owner's username and Actor name.",
          "required": true
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        }
      ]
    },
    {
      "name": "browser-info_tools-delete",
      "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized. Optional: skipHeaders, rawHeaders. Destructive. (public)",
      "method": "DELETE",
      "path": "/v2/browser-info",
      "no_auth": true,
      "params": [
        {
          "name": "skipHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response omits the `headers` field."
        },
        {
          "name": "rawHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
        }
      ]
    },
    {
      "name": "browser-info_tools-get",
      "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized. Optional: skipHeaders, rawHeaders. (public)",
      "method": "GET",
      "path": "/v2/browser-info",
      "no_auth": true,
      "params": [
        {
          "name": "skipHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response omits the `headers` field."
        },
        {
          "name": "rawHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
        }
      ]
    },
    {
      "name": "browser-info_tools-post",
      "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized. Optional: skipHeaders, rawHeaders. (public)",
      "method": "POST",
      "path": "/v2/browser-info",
      "no_auth": true,
      "params": [
        {
          "name": "skipHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response omits the `headers` field."
        },
        {
          "name": "rawHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
        }
      ]
    },
    {
      "name": "browser-info_tools-put",
      "description": "Returns information about the HTTP request, including the client IP address, country code, request headers, and body length. This endpoint is designed for proxy testing. It accepts any HTTP method so you can verify that your proxy correctly forwards requests of any type and that client IP addresses are anonymized. Optional: skipHeaders, rawHeaders. (public)",
      "method": "PUT",
      "path": "/v2/browser-info",
      "no_auth": true,
      "params": [
        {
          "name": "skipHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response omits the `headers` field."
        },
        {
          "name": "rawHeaders",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response includes the `rawHeaders` field with the raw request headers."
        }
      ]
    },
    {
      "name": "datasets_delete",
      "description": "Deletes a specific dataset. Required: datasetId. Destructive.",
      "method": "DELETE",
      "path": "/v2/datasets/{datasetId}",
      "params": [
        {
          "name": "datasetId",
          "type": "string",
          "location": "path",
          "description": "Dataset ID or `username~dataset-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "datasets_get",
      "description": "Lists all of a user's datasets. The response is a JSON array of objects, where each object contains basic information about one dataset. By default, the objects are sorted by the `createdAt` field in ascending order, therefore you can use pagination to incrementally fetch all datasets while new ones are still being created. To sort them in descending order, use `desc=1` parameter. The endpoint supports pagination using `limit` and `offset` parameters and it will not return more than 1000 array elements. Optional: offset, limit, desc (plus 2 more). Returns the ListOfDatasetsResponse.",
      "method": "GET",
      "path": "/v2/datasets",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        },
        {
          "name": "unnamed",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then all the storages are returned. By default, only named storages are returned."
        },
        {
          "name": "ownership",
          "type": "string",
          "location": "query",
          "description": "Filter by ownership. If this parameter is omitted, all accessible datasets are returned. - `ownedByMe`: Return only datasets owned by the user. - `sharedWithMe`: Return only datasets shared with the user by other users."
        }
      ]
    },
    {
      "name": "datasets_get-datasetid",
      "description": "Returns dataset object for given dataset ID. This does not return dataset items, only information about the storage itself. To retrieve dataset items, use the [List dataset items](/api/v2/dataset-items-get) endpoint. :::note Keep in mind that attributes `itemCount` and `cleanItemCount` are not propagated right away after data are pushed into a dataset. ::: There is a short period (up to 5 seconds) during which these counters may not match with exact counts in dataset items. Required: datasetId.",
      "method": "GET",
      "path": "/v2/datasets/{datasetId}",
      "params": [
        {
          "name": "datasetId",
          "type": "string",
          "location": "path",
          "description": "Dataset ID or `username~dataset-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "datasets_post",
      "description": "Creates a dataset and returns its object. Keep in mind that data stored under unnamed dataset follows [data retention period](https://docs.apify.com/platform/storage#data-retention). It creates a dataset with the given name if the parameter name is used. If a dataset with the given name already exists then returns its object. Optional: name.",
      "method": "POST",
      "path": "/v2/datasets",
      "params": [
        {
          "name": "name",
          "type": "string",
          "location": "query",
          "description": "Custom unique name to easily identify the dataset in the future."
        }
      ]
    },
    {
      "name": "datasets_put",
      "description": "Updates a dataset's name and general resource access level using a value specified by a JSON object passed in the PUT payload. The response is the updated dataset object, as returned by the [Get dataset](/api/v2/dataset-get) API endpoint. Required: datasetId. Optional: generalAccess, name. Returns the updated DatasetResponse.",
      "method": "PUT",
      "path": "/v2/datasets/{datasetId}",
      "params": [
        {
          "name": "datasetId",
          "type": "string",
          "location": "path",
          "description": "Dataset ID or `username~dataset-name`.",
          "required": true
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "datasets_items_dataset-get",
      "description": "Returns data stored in the dataset in a desired format. ### Response format The format of the response depends on \u003ccode\u003eformat\u003c/code\u003e query parameter. The \u003ccode\u003eformat\u003c/code\u003e parameter can have one of the following values: \u003ccode\u003ejson\u003c/code\u003e, \u003ccode\u003ejsonl\u003c/code\u003e, \u003ccode\u003exml\u003c/code\u003e, \u003ccode\u003ehtml\u003c/code\u003e, \u003ccode\u003ecsv\u003c/code\u003e, \u003ccode\u003exlsx\u003c/code\u003e and \u003ccode\u003erss\u003c/code\u003e. The following table describes how each format is treated. \u003ctable\u003e \u003ctr\u003e \u003cth\u003eFormat\u003c/th\u003e \u003cth\u003eItems\u003c/th\u003e \u003c/tr\u003e \u003ctr\u003e \u003ctd\u003e\u003ccode\u003ejson\u003c/code\u003e\u003c/td\u003e \u003ctd rowspan='3'\u003eThe response is a JSON, JSONL or XML array of raw item objects.\u003c/td\u003e \u003c/tr\u003e \u003ctr\u003e \u003ctd\u003e\u003ccode\u003ejsonl\u003c/code\u003e\u003c/td\u003e \u003c/tr\u003e \u003ctr\u003e \u003ctd\u003e\u003ccode\u003exml\u003c/code\u003e\u003c/td\u003e \u003c/tr\u003e \u003ctr\u003e \u003ctd\u003e\u003ccode\u003ehtml\u003c/code\u003e\u003c/td\u003e \u003ctd rowspan='3'\u003eThe response is a HTML, CSV or XLSX table, where columns correspond to the properties of the item and rows correspond to each dataset item.\u003c/td\u003e \u003c/tr\u003e \u003ctr\u003e \u003ctd\u003e\u003ccode\u003ecsv\u003c/code\u003e\u003c/td\u003e \u003c/tr\u003e \u003ctr\u003e \u003ctd\u003e\u003ccode\u003exlsx\u003c/code\u003e\u003c/td\u003e \u003c/tr\u003e \u003ctr\u003e \u003ctd\u003e\u003ccode\u003erss\u003c/code\u003e\u003c/td\u003e \u003ctd colspan='2'\u003eThe response is a RSS file. Each item is displayed as child elements of one \u003ccode\u003e\u0026lt;item\u0026gt;\u003c/code\u003e.\u003c/td\u003e \u003c/tr\u003e \u003c/table\u003e Note that CSV, XLSX and HTML tables are limited to 2000 columns and the column names cannot be longer than 200 characters. JSON, XML and RSS formats do not have such restrictions. ### Hidden fields The top-level fields starting with the `#` character are considered hidden. These are useful to store debugging information and can be omitted from the output by providing the `skipHidden=1` or `clean=1` query parameters. For example, if you store the following object to the dataset: ``` { productName: 'iPhone Xs', description: 'Welcome to the big screens.' #debug: { url: 'https://www.apple.com/lae/iphone-xs/', crawledAt: '2019-01-21T16:06:03.683Z' } } ``` The `#debug` field will be considered as hidden and can be omitted from the results. This is useful to provide nice cleaned data to end users, while keeping debugging info available if needed. The Dataset object returned by the API contains the number of such clean items in the`dataset.cleanItemCount` property. ### XML format extension When exporting results to XML or RSS formats, the names of object properties become XML tags and the corresponding values become tag's children. For example, the following JavaScript object: ``` { name: 'Paul Newman', address: [ { type: 'home', street: '21st', city: 'Chicago' }, { type: 'office', street: null, city: null } ] } ``` will be transformed to the following XML snippet: ``` \u003cname\u003ePaul Newman\u003c/name\u003e \u003caddress\u003e \u003ctype\u003ehome\u003c/type\u003e \u003cstreet\u003e21st\u003c/street\u003e \u003ccity\u003eChicago\u003c/city\u003e \u003c/address\u003e \u003caddress\u003e \u003ctype\u003eoffice\u003c/type\u003e \u003cstreet/\u003e \u003ccity/\u003e \u003c/address\u003e ``` If the JavaScript object contains a property named `@` then its sub-properties are exported as attributes of the parent XML element. If the parent XML element does not have any child elements then its value is taken from a JavaScript object property named `#`. For example, the following JavaScript object: ``` { 'address': [{ '@': { 'type': 'home' }, 'street': '21st', 'city': 'Chicago' }, { '@': { 'type': 'office' }, '#': 'unknown' }] } ``` will be transformed to the following XML snippet: ``` \u003caddress type='home'\u003e \u003cstreet\u003e21st\u003c/street\u003e \u003ccity\u003eChicago\u003c/city\u003e \u003c/address\u003e \u003caddress type='office'\u003eunknown\u003c/address\u003e ``` This feature is also useful to customize your RSS feeds generated for various websites. By default the whole result is wrapped in a `\u003citems\u003e` element and each page object is wrapped in a `\u003citem\u003e` element. You can change this using \u003ccode\u003exmlRoot\u003c/code\u003e and \u003ccode\u003exmlRow\u003c/code\u003e url parameters. ### Pagination The generated response supports [pagination](#/introduction/pagination). The pagination is always performed with the granularity of a single item, regardless whether \u003ccode\u003eunwind\u003c/code\u003e parameter was provided. By default, the **Items** in the response are sorted by the time they were stored to the database, therefore you can use pagination to incrementally fetch the items as they are being added. No limit exists to how many items can be returned in one response. If you specify `desc=1` query parameter, the results are returned in the reverse order than they were stored (i.e. from newest to oldest items). Note that only the order of **Items** is reversed, but not the order of the `unwind` array elements. Required: datasetId. Optional: format, clean, offset (plus 21 more).",
      "method": "GET",
      "path": "/v2/datasets/{datasetId}/items",
      "params": [
        {
          "name": "datasetId",
          "type": "string",
          "location": "path",
          "description": "Dataset ID or `username~dataset-name`.",
          "required": true
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "datasets_items_dataset-head",
      "description": "Returns only the HTTP headers for the dataset items endpoint, without the response body. This is useful to check pagination metadata or verify access without downloading the full dataset. Required: datasetId. Optional: format, clean, offset (plus 21 more).",
      "method": "HEAD",
      "path": "/v2/datasets/{datasetId}/items",
      "params": [
        {
          "name": "datasetId",
          "type": "string",
          "location": "path",
          "description": "Dataset ID or `username~dataset-name`.",
          "required": true
        },
        {
          "name": "format",
          "type": "string",
          "location": "query",
          "description": "Format of the results, possible values are: `json`, `jsonl`, `csv`, `html`, `xlsx`, `xml`..."
        },
        {
          "name": "clean",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the API endpoint returns only non-empty items and skips hidden fields (i."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. By default there is no limit."
        },
        {
          "name": "fields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be picked from the items, only these fields..."
        },
        {
          "name": "outputFields",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of output field names that positionally rename the fields specified..."
        },
        {
          "name": "omit",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be omitted from the items."
        },
        {
          "name": "unwind",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should be unwound, in order which they should be..."
        },
        {
          "name": "flatten",
          "type": "string",
          "location": "query",
          "description": "A comma-separated list of fields which should transform nested objects into flat structures."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "By default, results are returned in the same order as they were stored."
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the response will define the `Content-Disposition: attachment` header,..."
        },
        {
          "name": "delimiter",
          "type": "string",
          "location": "query",
          "description": "A delimiter character for CSV files, only used if `format=csv`."
        },
        {
          "name": "bom",
          "type": "bool",
          "location": "query",
          "description": "All text responses are encoded in UTF-8 encoding."
        },
        {
          "name": "xmlRoot",
          "type": "string",
          "location": "query",
          "description": "Overrides default root element name of `xml` output. By default the root element is `items`."
        },
        {
          "name": "xmlRow",
          "type": "string",
          "location": "query",
          "description": "Overrides default element name that wraps each page or page function result object in `xml`..."
        },
        {
          "name": "skipHeaderRow",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then header row in the `csv` format is skipped."
        },
        {
          "name": "skipHidden",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then hidden fields are skipped from the output, i."
        },
        {
          "name": "skipEmpty",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then empty items are skipped from the output."
        },
        {
          "name": "simplified",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the endpoint applies the `fields=url,pageFunctionResult,errorInfo`..."
        },
        {
          "name": "view",
          "type": "string",
          "location": "query",
          "description": "Defines the view configuration for dataset items based on the schema definition."
        },
        {
          "name": "skipFailedPages",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then, the all the items with errorInfo property will be skipped from the..."
        },
        {
          "name": "feedTitle",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003ctitle\u003e` element."
        },
        {
          "name": "feedDescription",
          "type": "string",
          "location": "query",
          "description": "Overrides the auto-generated RSS channel `\u003cdescription\u003e` element."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "datasets_items_dataset-post",
      "description": "Appends an item or an array of items to the end of the dataset. The POST payload is a JSON object or a JSON array of objects to save into the dataset. If the data you attempt to store in the dataset is invalid (meaning any of the items received by the API fails the validation), the whole request is discarded and the API will return a response with status code 400. For more information about dataset schema validation, see [Dataset schema](https://docs.apify.com/platform/actors/development/actor-definition/dataset-schema/validation). **IMPORTANT:** The limit of request payload size for the dataset is 5 MB. If the array exceeds the size, you'll need to split it into a number of smaller arrays. Required: datasetId. Returns the new ItemsDatasetPostResponse.",
      "method": "POST",
      "path": "/v2/datasets/{datasetId}/items",
      "params": [
        {
          "name": "datasetId",
          "type": "string",
          "location": "path",
          "description": "Dataset ID or `username~dataset-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "datasets_statistics_dataset-get",
      "description": "Returns statistics for given dataset. Provides only [field statistics](https://docs.apify.com/platform/actors/development/actor-definition/dataset-schema/validation#dataset-field-statistics). Required: datasetId.",
      "method": "GET",
      "path": "/v2/datasets/{datasetId}/statistics",
      "params": [
        {
          "name": "datasetId",
          "type": "string",
          "location": "path",
          "description": "Dataset ID or `username~dataset-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "key-value-stores_delete",
      "description": "Deletes a key-value store. Required: storeId. Destructive.",
      "method": "DELETE",
      "path": "/v2/key-value-stores/{storeId}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "key-value-stores_get",
      "description": "Gets the list of key-value stores owned by the user. The response is a list of objects, where each objects contains a basic information about a single key-value store. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 array elements. By default, the records are sorted by the `createdAt` field in ascending order, therefore you can use pagination to incrementally fetch all key-value stores while new ones are still being created. To sort the records in descending order, use the `desc=1` parameter. Optional: offset, limit, desc (plus 2 more). Returns the ListOfKeyValueStoresResponse.",
      "method": "GET",
      "path": "/v2/key-value-stores",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        },
        {
          "name": "unnamed",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then all the storages are returned. By default, only named storages are returned."
        },
        {
          "name": "ownership",
          "type": "string",
          "location": "query",
          "description": "Filter by ownership. If this parameter is omitted, all accessible key-value stores are returned. - `ownedByMe`: Return only key-value stores owned by the user. - `sharedWithMe`: Return only key-value stores shared with the user by other users."
        }
      ]
    },
    {
      "name": "key-value-stores_get-keyvaluestores",
      "description": "Gets an object that contains all the details about a specific key-value store. Required: storeId. Returns the KeyValueStoreResponse.",
      "method": "GET",
      "path": "/v2/key-value-stores/{storeId}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "key-value-stores_post",
      "description": "Creates a key-value store and returns its object. The response is the same object as returned by the [Get store](#/reference/key-value-stores/store-object/get-store) endpoint. Keep in mind that data stored under unnamed store follows [data retention period](https://docs.apify.com/platform/storage#data-retention). It creates a store with the given name if the parameter name is used. If there is another store with the same name, the endpoint does not create a new one and returns the existing object instead. Optional: name.",
      "method": "POST",
      "path": "/v2/key-value-stores",
      "params": [
        {
          "name": "name",
          "type": "string",
          "location": "query",
          "description": "Custom unique name to easily identify the store in the future."
        }
      ]
    },
    {
      "name": "key-value-stores_put",
      "description": "Updates a key-value store's name and general resource access level using a value specified by a JSON object passed in the PUT payload. The response is the updated key-value store object, as returned by the [Get store](#/reference/key-value-stores/store-object/get-store) API endpoint. Required: storeId. Optional: generalAccess, name. Returns the updated KeyValueStoreResponse.",
      "method": "PUT",
      "path": "/v2/key-value-stores/{storeId}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        }
      ]
    },
    {
      "name": "key-value-stores_keys_value-store-get",
      "description": "Returns a list of objects describing keys of a given key-value store, as well as some information about the values (e.g. size). This endpoint is paginated using `exclusiveStartKey` and `limit` parameters - see [Pagination](/api/v2#using-key) for more details. Required: storeId. Optional: exclusiveStartKey, limit (default: 1000), collection (plus 2 more).",
      "method": "GET",
      "path": "/v2/key-value-stores/{storeId}/keys",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "exclusiveStartKey",
          "type": "string",
          "location": "query",
          "description": "All keys up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned."
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that belong to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "Limit the results to keys that start with a specific prefix."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "key-value-stores_records_key-value-store-delete",
      "description": "Removes a record specified by a key from the key-value store. Required: storeId, recordKey. Destructive.",
      "method": "DELETE",
      "path": "/v2/key-value-stores/{storeId}/records/{recordKey}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "key-value-stores_records_key-value-store-get",
      "description": "Downloads all records from the key-value store as a ZIP archive. Each record is stored as a separate file in the archive, with the filename equal to the record key. You can optionally filter the records by `collection` or `prefix` to download only a subset of the store. Required: storeId. Optional: collection, prefix, signature.",
      "method": "GET",
      "path": "/v2/key-value-stores/{storeId}/records",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "collection",
          "type": "string",
          "location": "query",
          "description": "If specified, only records belonging to a specific collection from the key-value store schema."
        },
        {
          "name": "prefix",
          "type": "string",
          "location": "query",
          "description": "If specified, only records whose key starts with the given prefix are included in the archive."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "key-value-stores_records_key-value-store-get-keyvaluestores",
      "description": "Gets a value stored in the key-value store under a specific key. The response body has the same `Content-Encoding` header as it was set in [Put record](#tag/Key-value-storesRecord/operation/keyValueStore_record_put). If the request does not define the `Accept-Encoding` HTTP header with the right encoding, the record will be decompressed. Most HTTP clients support decompression by default. After using the HTTP client with decompression support, the `Accept-Encoding` header is set by the client and body is decompressed automatically. Please note that for security reasons, Apify API can perform small modifications to HTML documents before they are served via this endpoint. To fetch the raw HTML content without any modifications, use the `attachment` query parameter. Required: storeId, recordKey. Optional: attachment, signature. Returns the RecordResponse.",
      "method": "GET",
      "path": "/v2/key-value-stores/{storeId}/records/{recordKey}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        },
        {
          "name": "attachment",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the response will be served with `Content-Disposition: attachment` header,..."
        },
        {
          "name": "signature",
          "type": "string",
          "location": "query",
          "description": "Signature used for the access."
        }
      ]
    },
    {
      "name": "key-value-stores_records_key-value-store-head",
      "description": "Check if a value is stored in the key-value store under a specific key. Required: storeId, recordKey.",
      "method": "HEAD",
      "path": "/v2/key-value-stores/{storeId}/records/{recordKey}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "key-value-stores_records_key-value-store-post",
      "description": "Stores a value under a specific key to the key-value store. This endpoint is an alias for the [`PUT` record](#tag/Key-value-storesRecord/operation/keyValueStore_record_put) method and behaves identically. Required: storeId, recordKey. Returns the new RecordsKeyValueStorePostResponse.",
      "method": "POST",
      "path": "/v2/key-value-stores/{storeId}/records/{recordKey}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "key-value-stores_records_key-value-store-put",
      "description": "Stores a value under a specific key to the key-value store. The value is passed as the PUT payload and it is stored with a MIME content type defined by the `Content-Type` header and with encoding defined by the `Content-Encoding` header. To save bandwidth, storage, and speed up your upload, send the request payload compressed with Gzip compression and add the `Content-Encoding: gzip` header. It is possible to set up another compression type with `Content-Encoding` request header. Below is a list of supported `Content-Encoding` types. * Gzip compression: `Content-Encoding: gzip` * Deflate compression: `Content-Encoding: deflate` * Brotli compression: `Content-Encoding: br`. Required: storeId, recordKey. Returns the updated RecordsKeyValueStorePutResponse.",
      "method": "PUT",
      "path": "/v2/key-value-stores/{storeId}/records/{recordKey}",
      "params": [
        {
          "name": "storeId",
          "type": "string",
          "location": "path",
          "description": "Key-value store ID or `username~store-name`.",
          "required": true
        },
        {
          "name": "recordKey",
          "type": "string",
          "location": "path",
          "description": "Key of the record.",
          "required": true
        }
      ]
    },
    {
      "name": "logs_get",
      "description": "Retrieves logs for a specific Actor build or run. Required: buildOrRunId. Optional: stream, download, raw. (public)",
      "method": "GET",
      "path": "/v2/logs/{buildOrRunId}",
      "no_auth": true,
      "params": [
        {
          "name": "buildOrRunId",
          "type": "string",
          "location": "path",
          "description": "ID of the Actor build or run.",
          "required": true
        },
        {
          "name": "stream",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the logs will be streamed as long as the run or build is running."
        },
        {
          "name": "download",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the web browser will download the log file rather than open it in a tab."
        },
        {
          "name": "raw",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1`, the logs will be kept verbatim."
        }
      ]
    },
    {
      "name": "request-queues_delete",
      "description": "Deletes given queue. Required: queueId. Destructive.",
      "method": "DELETE",
      "path": "/v2/request-queues/{queueId}",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "request-queues_get",
      "description": "Lists all of a user's request queues. The response is a JSON array of objects, where each object contains basic information about one queue. By default, the objects are sorted by the `createdAt` field in ascending order, therefore you can use pagination to incrementally fetch all queues while new ones are still being created. To sort them in descending order, use `desc=1` parameter. The endpoint supports pagination using `limit` and `offset` parameters and it will not return more than 1000 array elements. Optional: offset, limit, desc (plus 2 more). Returns the ListOfRequestQueuesResponse.",
      "method": "GET",
      "path": "/v2/request-queues",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        },
        {
          "name": "unnamed",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then all the storages are returned. By default, only named storages are returned."
        },
        {
          "name": "ownership",
          "type": "string",
          "location": "query",
          "description": "Filter by ownership. If this parameter is omitted, all accessible request queues are returned. - `ownedByMe`: Return only request queues owned by the user. - `sharedWithMe`: Return only request queues shared with the user by other users."
        }
      ]
    },
    {
      "name": "request-queues_get-requestqueues",
      "description": "Returns queue object for given queue ID. Required: queueId.",
      "method": "GET",
      "path": "/v2/request-queues/{queueId}",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        }
      ]
    },
    {
      "name": "request-queues_post",
      "description": "Creates a request queue and returns its object. Keep in mind that requests stored under unnamed queue follows [data retention period](https://docs.apify.com/platform/storage#data-retention). It creates a queue of given name if the parameter name is used. If a queue with the given name already exists then the endpoint returns its object. Optional: name.",
      "method": "POST",
      "path": "/v2/request-queues",
      "params": [
        {
          "name": "name",
          "type": "string",
          "location": "query",
          "description": "Custom unique name to easily identify the queue in the future."
        }
      ]
    },
    {
      "name": "request-queues_put",
      "description": "Updates a request queue's name and general resource access level using a value specified by a JSON object passed in the PUT payload. The response is the updated request queue object, as returned by the [Get request queue](#/reference/request-queues/queue-collection/get-request-queue) API endpoint. Required: queueId. Optional: generalAccess, name. Returns the updated RequestQueueResponse.",
      "method": "PUT",
      "path": "/v2/request-queues/{queueId}",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "generalAccess",
          "type": "string",
          "location": "body",
          "description": "Defines the general access level for the resource."
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "The new name for the request queue."
        }
      ]
    },
    {
      "name": "request-queues_head_request-queue-get",
      "description": "Returns given number of first requests from the queue. The response contains the `hadMultipleClients` boolean field which indicates that the queue was accessed by more than one client (with unique or empty `clientKey`). This field is used by [Apify SDK](https://sdk.apify.com) to determine whether the local cache is consistent with the request queue, and thus optimize performance of certain operations. Required: queueId. Optional: limit, clientKey.",
      "method": "GET",
      "path": "/v2/request-queues/{queueId}/head",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "request-queues_head_request-queue-lock-post",
      "description": "Returns the given number of first requests from the queue and locks them for the given time. If this endpoint locks the request, no other client or run will be able to get and lock these requests. The response contains the `hadMultipleClients` boolean field which indicates that the queue was accessed by more than one client (with unique or empty `clientKey`). Required: queueId, lockSecs. Optional: limit, clientKey.",
      "method": "POST",
      "path": "/v2/request-queues/{queueId}/head/lock",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "How many items from the queue should be returned."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-batch-delete",
      "description": "Batch-deletes given requests from the queue. The number of requests in a batch is limited to 25. The response contains an array of unprocessed and processed requests. If any delete operation fails because the request queue rate limit is exceeded or an internal failure occurs, the failed request is returned in the `unprocessedRequests` response parameter. You can re-send these delete requests. It is recommended to use an exponential backoff algorithm for these retries. Each request is identified by its ID or uniqueKey parameter. You can use either of them to identify the request. Required: queueId. Optional: clientKey. Returns the BatchDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/request-queues/{queueId}/requests/batch",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-batch-post",
      "description": "Adds requests to the queue in batch. The maximum requests in batch is limited to 25. The response contains an array of unprocessed and processed requests. If any add operation fails because the request queue rate limit is exceeded or an internal failure occurs, the failed request is returned in the unprocessedRequests response parameter. You can resend these requests to add. It is recommended to use an exponential backoff algorithm for these retries. If a request with the same `uniqueKey` was already present in the queue, then it returns an ID of the existing request. Required: queueId. Optional: clientKey, forefront.",
      "method": "POST",
      "path": "/v2/request-queues/{queueId}/requests/batch",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-delete",
      "description": "Deletes given request from queue. Required: queueId, requestId. Optional: clientKey. Destructive.",
      "method": "DELETE",
      "path": "/v2/request-queues/{queueId}/requests/{requestId}",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-get",
      "description": "Returns a list of requests. This endpoint is paginated using cursor (pagination by `exclusiveStartId` is deprecated) and limit parameters. Required: queueId. Optional: clientKey, exclusiveStartId, limit (plus 2 more).",
      "method": "GET",
      "path": "/v2/request-queues/{queueId}/requests",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "exclusiveStartId",
          "type": "string",
          "location": "query",
          "description": "All requests up to this one (including) are skipped from the result."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Number of keys to be returned. Maximum value is `10000`."
        },
        {
          "name": "cursor",
          "type": "string",
          "location": "query",
          "description": "A cursor string for pagination, returned in the previous response as `nextCursor`."
        },
        {
          "name": "filter",
          "type": "array",
          "location": "query",
          "description": "Filter requests by their state."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-get-requestqueues",
      "description": "Returns request from queue. Required: queueId, requestId.",
      "method": "GET",
      "path": "/v2/request-queues/{queueId}/requests/{requestId}",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-lock-delete",
      "description": "Deletes a request lock. The request lock can be deleted only by the client that has locked it using [Get and lock head operation](#/request-queue-head-lock-post). The clientKey identifier is used for locking and unlocking requests. You can delete or prolong the lock only for requests that were locked by the same client key or from the same Actor run. Required: queueId, requestId. Optional: clientKey, forefront. Destructive.",
      "method": "DELETE",
      "path": "/v2/request-queues/{queueId}/requests/{requestId}/lock",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock was..."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-lock-put",
      "description": "Prolongs request lock. The request lock can be prolonged only by the client that has locked it using [Get and lock head operation](#/request-queue-head-lock-post). The clientKey identifier is used for locking and unlocking requests. You can delete or prolong the lock only for requests that were locked by the same client key or from the same Actor run. Required: queueId, requestId, lockSecs. Optional: clientKey, forefront. Returns the updated ProlongRequestLockResponse.",
      "method": "PUT",
      "path": "/v2/request-queues/{queueId}/requests/{requestId}/lock",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "lockSecs",
          "type": "float",
          "location": "query",
          "description": "How long the requests will be locked for (in seconds).",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end after lock expires."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-post",
      "description": "Adds request to the queue. Response contains ID of the request and info if request was already present in the queue or handled. If request with same `uniqueKey` was already present in the queue then returns an ID of existing request. Required: queueId. Optional: clientKey, forefront, errorMessages (plus 10 more).",
      "method": "POST",
      "path": "/v2/request-queues/{queueId}/requests",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication."
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request."
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-put",
      "description": "Updates a request in a queue. Mark request as handled by setting `request.handledAt = new Date()`. If `handledAt` is set, the request will be removed from head of the queue (and unlocked, if applicable). Required: queueId, requestId, id, uniqueKey, url. Optional: forefront, clientKey, errorMessages (plus 8 more). Returns the updated UpdateRequestResponse.",
      "method": "PUT",
      "path": "/v2/request-queues/{queueId}/requests/{requestId}",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "requestId",
          "type": "string",
          "location": "path",
          "description": "Request ID.",
          "required": true
        },
        {
          "name": "forefront",
          "type": "string",
          "location": "query",
          "description": "Determines if request should be added to the head of the queue or to the end."
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        },
        {
          "name": "errorMessages",
          "type": "array",
          "location": "body",
          "description": "Error messages recorded from failed processing attempts."
        },
        {
          "name": "handledAt",
          "type": "string",
          "location": "body",
          "description": "The timestamp when the request was marked as handled, if applicable."
        },
        {
          "name": "headers",
          "type": "object",
          "location": "body",
          "description": "HTTP headers sent with the request."
        },
        {
          "name": "id",
          "type": "string",
          "location": "body",
          "description": "A unique identifier assigned to the request.",
          "required": true
        },
        {
          "name": "loadedUrl",
          "type": "string",
          "location": "body",
          "description": "The final URL that was loaded, after redirects (if any)."
        },
        {
          "name": "method",
          "type": "string",
          "location": "body",
          "description": "Method"
        },
        {
          "name": "noRetry",
          "type": "bool",
          "location": "body",
          "description": "Indicates whether the request should not be retried if processing fails."
        },
        {
          "name": "payload",
          "type": "object",
          "location": "body",
          "description": "The request payload, typically used with POST or PUT requests."
        },
        {
          "name": "retryCount",
          "type": "int",
          "location": "body",
          "description": "The number of times this request has been retried."
        },
        {
          "name": "uniqueKey",
          "type": "string",
          "location": "body",
          "description": "A unique key used for request de-duplication.",
          "required": true
        },
        {
          "name": "url",
          "type": "string",
          "location": "body",
          "description": "The URL of the request.",
          "required": true
        },
        {
          "name": "userData",
          "type": "object",
          "location": "body",
          "description": "Custom user data attached to the request. Can contain arbitrary fields."
        }
      ]
    },
    {
      "name": "request-queues_requests_queue-unlock-post",
      "description": "Unlocks requests in the queue that are currently locked by the client. * If the client is within an Actor run, it unlocks all requests locked by that specific run plus all requests locked by the same clientKey. * If the client is outside of an Actor run, it unlocks all requests locked using the same clientKey. Required: queueId. Optional: clientKey. Returns the new UnlockRequestsResponse.",
      "method": "POST",
      "path": "/v2/request-queues/{queueId}/requests/unlock",
      "params": [
        {
          "name": "queueId",
          "type": "string",
          "location": "path",
          "description": "Queue ID or `username~queue-name`.",
          "required": true
        },
        {
          "name": "clientKey",
          "type": "string",
          "location": "query",
          "description": "A unique identifier of the client accessing the request queue."
        }
      ]
    },
    {
      "name": "schedules_delete",
      "description": "Deletes a schedule. Required: scheduleId. Returns the SchedulesDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/schedules/{scheduleId}",
      "params": [
        {
          "name": "scheduleId",
          "type": "string",
          "location": "path",
          "description": "Schedule ID.",
          "required": true
        }
      ]
    },
    {
      "name": "schedules_get",
      "description": "Gets the list of schedules that the user created. The endpoint supports pagination using the `limit` and `offset` parameters. It will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order. To sort the records in descending order, use the `desc=1` parameter. Optional: offset, limit, desc. Returns the ListOfSchedulesResponse.",
      "method": "GET",
      "path": "/v2/schedules",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        }
      ]
    },
    {
      "name": "schedules_get-scheduleid",
      "description": "Gets the schedule object with all details. Required: scheduleId. Returns the ScheduleResponse.",
      "method": "GET",
      "path": "/v2/schedules/{scheduleId}",
      "params": [
        {
          "name": "scheduleId",
          "type": "string",
          "location": "path",
          "description": "Schedule ID.",
          "required": true
        }
      ]
    },
    {
      "name": "schedules_post",
      "description": "Creates a new schedule with settings provided by the schedule object passed as JSON in the payload. The response is the created schedule object. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). Optional: actions, cronExpression, description (plus 5 more). Returns the new ScheduleResponse.",
      "method": "POST",
      "path": "/v2/schedules",
      "params": [
        {
          "name": "actions",
          "type": "array",
          "location": "body",
          "description": "Actions"
        },
        {
          "name": "cronExpression",
          "type": "string",
          "location": "body",
          "description": "Cron expression"
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "Description"
        },
        {
          "name": "isEnabled",
          "type": "bool",
          "location": "body",
          "description": "Is enabled"
        },
        {
          "name": "isExclusive",
          "type": "bool",
          "location": "body",
          "description": "Is exclusive"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        },
        {
          "name": "timezone",
          "type": "string",
          "location": "body",
          "description": "Timezone"
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "Title"
        }
      ]
    },
    {
      "name": "schedules_put",
      "description": "Updates a schedule using values specified by a schedule object passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The response is the full schedule object as returned by the [Get schedule](#/reference/schedules/schedule-object/get-schedule) endpoint. **The request needs to specify the `Content-Type: application/json` HTTP header!** When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). Required: scheduleId. Optional: actions, cronExpression, description (plus 5 more). Returns the updated ScheduleResponse.",
      "method": "PUT",
      "path": "/v2/schedules/{scheduleId}",
      "params": [
        {
          "name": "scheduleId",
          "type": "string",
          "location": "path",
          "description": "Schedule ID.",
          "required": true
        },
        {
          "name": "actions",
          "type": "array",
          "location": "body",
          "description": "Actions"
        },
        {
          "name": "cronExpression",
          "type": "string",
          "location": "body",
          "description": "Cron expression"
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "Description"
        },
        {
          "name": "isEnabled",
          "type": "bool",
          "location": "body",
          "description": "Is enabled"
        },
        {
          "name": "isExclusive",
          "type": "bool",
          "location": "body",
          "description": "Is exclusive"
        },
        {
          "name": "name",
          "type": "string",
          "location": "body",
          "description": "Name"
        },
        {
          "name": "timezone",
          "type": "string",
          "location": "body",
          "description": "Timezone"
        },
        {
          "name": "title",
          "type": "string",
          "location": "body",
          "description": "Title"
        }
      ]
    },
    {
      "name": "schedules_log_schedule-get",
      "description": "Gets the schedule log as a JSON array containing information about up to a 1000 invocations of the schedule. Required: scheduleId. Returns array of ScheduleInvoked.",
      "method": "GET",
      "path": "/v2/schedules/{scheduleId}/log",
      "params": [
        {
          "name": "scheduleId",
          "type": "string",
          "location": "path",
          "description": "Schedule ID.",
          "required": true
        }
      ]
    },
    {
      "name": "store_get",
      "description": "Gets the list of public Actors in Apify Store. You can use `search` parameter to search Actors by string in title, name, description, username and readme. If you need detailed info about a specific Actor, use the [Get Actor](#/reference/actors/actor-object/get-actor) endpoint. The endpoint supports pagination using the `limit` and `offset` parameters. It will not return more than 1,000 records. Optional: limit, offset, search (plus 7 more). Returns the ListOfActorsInStoreResponse.",
      "method": "GET",
      "path": "/v2/store",
      "params": [
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "search",
          "type": "string",
          "location": "query",
          "description": "String to search by. The search runs on the following fields: `title`, `name`, `description`, `username`, `readme`."
        },
        {
          "name": "sortBy",
          "type": "string",
          "location": "query",
          "description": "Specifies the field by which to sort the results. The supported values are `relevance` (default), `popularity`, `newest` and `lastUpdate`."
        },
        {
          "name": "category",
          "type": "string",
          "location": "query",
          "description": "Filters the results by the specified category."
        },
        {
          "name": "username",
          "type": "string",
          "location": "query",
          "description": "Filters the results by the specified username."
        },
        {
          "name": "pricingModel",
          "type": "string",
          "location": "query",
          "description": "Only return Actors with the specified pricing model."
        },
        {
          "name": "allowsAgenticUsers",
          "type": "bool",
          "location": "query",
          "description": "If true, only return Actors that allow agentic users. If false, only return Actors that do not allow agentic users."
        },
        {
          "name": "responseFormat",
          "type": "string",
          "location": "query",
          "description": "Controls the shape of the response. Use `full` (default) for the complete response including image URLs and all fields. Use `agent` for a reduced field set optimized for LLM consumers, which only includes `id`, `title`, `name`, `username`, `description`, `notice`, `badge`, `categories`, and minimal `stats`."
        },
        {
          "name": "includeUnrunnableActors",
          "type": "bool",
          "location": "query",
          "description": "By default, search results exclude Actors that are not safe to run automatically (e.g. Actors from developers who haven't passed KYC, or full-permission Actors without a large user base). Set to `true` to bypass this safety filtering and include all Actors in the results."
        }
      ]
    },
    {
      "name": "tools_decode-and-verify-post",
      "description": "Decodes and verifies an encoded value previously created by the encode-and-sign endpoint. Returns the original decoded object along with information about the user who encoded it and whether that user is verified. **Important**: The request must specify the `Content-Type: application/json` HTTP header. Required: encoded. (public)",
      "method": "POST",
      "path": "/v2/tools/decode-and-verify",
      "no_auth": true,
      "params": [
        {
          "name": "encoded",
          "type": "string",
          "location": "body",
          "description": "Encoded",
          "required": true
        }
      ]
    },
    {
      "name": "tools_encode-and-sign-post",
      "description": "Encodes and signs any JSON object. The encoded value includes a signature tied to the authenticated user's ID, which can later be verified using the decode-and-verify endpoint. **Important**: The request must specify the `Content-Type: application/json` HTTP header. Returns the new EncodeAndSignResponse.",
      "method": "POST",
      "path": "/v2/tools/encode-and-sign",
      "params": []
    },
    {
      "name": "users_get",
      "description": "Returns public information about a specific user account, similar to what can be seen on public profile pages (e.g. https://apify.com/apify). This operation requires no authentication token. Required: userId.",
      "method": "GET",
      "path": "/v2/users/{userId}",
      "params": [
        {
          "name": "userId",
          "type": "string",
          "location": "path",
          "description": "User ID or username.",
          "required": true
        }
      ]
    },
    {
      "name": "users_me-get",
      "description": "Returns information about the current user account, including both public and private information. The user account is identified by the provided authentication token. The fields `plan`, `email` and `profile` are omitted when this endpoint is accessed from Actor run.",
      "method": "GET",
      "path": "/v2/users/me",
      "params": []
    },
    {
      "name": "users_me-limits-get",
      "description": "Returns a complete summary of your account's limits. It is the same information you will see on your account's [Limits page](https://console.apify.com/billing#/limits). The returned data includes the current usage cycle, a summary of your limits, and your current usage.",
      "method": "GET",
      "path": "/v2/users/me/limits",
      "params": []
    },
    {
      "name": "users_me-limits-put",
      "description": "Updates the account's limits manageable on your account's [Limits page](https://console.apify.com/billing#/limits). Specifically the: `maxMonthlyUsageUsd` and `dataRetentionDays` limits (see request body schema for more details). Optional: dataRetentionDays, maxMonthlyUsageUsd. Returns the updated UsersMeLimitsPutResponse.",
      "method": "PUT",
      "path": "/v2/users/me/limits",
      "params": [
        {
          "name": "dataRetentionDays",
          "type": "int",
          "location": "body",
          "description": "Apify securely stores your ten most recent Actor runs indefinitely, ensuring they are always accessible. Unnamed storages and other Actor runs are automatically deleted after the retention period. If you're subscribed, you can change it to keep data for longer or to limit your usage. [Lear more](https://docs.apify.com/platform/storage/usage#data-retention)."
        },
        {
          "name": "maxMonthlyUsageUsd",
          "type": "float",
          "location": "body",
          "description": "If your platform usage in the billing period exceeds the prepaid usage, you will be charged extra. Setting this property you can update your hard limit on monthly platform usage to prevent accidental overage or to limit the extra charges."
        }
      ]
    },
    {
      "name": "users_me-usage-monthly-get",
      "description": "Returns a complete summary of your usage for the current monthly usage cycle, an overall sum, as well as a daily breakdown of usage. It is the same information you will see on your account's [Billing \u003e Historical usage page](https://console.apify.com/billing/historical-usage). The information includes your use of Actors, compute, data transfer, and storage. Using the `date` parameter will show your usage in the monthly usage cycle that includes that date. Optional: date.",
      "method": "GET",
      "path": "/v2/users/me/usage/monthly",
      "params": [
        {
          "name": "date",
          "type": "string",
          "location": "query",
          "description": "Date in the YYYY-MM-DD format."
        }
      ]
    },
    {
      "name": "webhook-dispatches_get",
      "description": "Gets the list of webhook dispatches that the user have. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order. To sort the records in descending order, use the `desc=1` parameter. Optional: offset, limit, desc. Returns the ListOfWebhookDispatchesResponse.",
      "method": "GET",
      "path": "/v2/webhook-dispatches",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        }
      ]
    },
    {
      "name": "webhook-dispatches_webhook-dispatch-get",
      "description": "Gets webhook dispatch object with all details. Required: dispatchId. Returns the WebhookDispatchResponse.",
      "method": "GET",
      "path": "/v2/webhook-dispatches/{dispatchId}",
      "params": [
        {
          "name": "dispatchId",
          "type": "string",
          "location": "path",
          "description": "Webhook dispatch ID.",
          "required": true
        }
      ]
    },
    {
      "name": "webhooks_delete",
      "description": "Deletes a webhook. Required: webhookId. Returns the WebhooksDeleteResponse. Destructive.",
      "method": "DELETE",
      "path": "/v2/webhooks/{webhookId}",
      "params": [
        {
          "name": "webhookId",
          "type": "string",
          "location": "path",
          "description": "Webhook ID.",
          "required": true
        }
      ]
    },
    {
      "name": "webhooks_get",
      "description": "Gets the list of webhooks that the user created. The endpoint supports pagination using the `limit` and `offset` parameters and it will not return more than 1000 records. By default, the records are sorted by the `createdAt` field in ascending order. To sort the records in descending order, use the `desc=1` parameter. Optional: offset, limit, desc. Returns the ListOfWebhooksResponse.",
      "method": "GET",
      "path": "/v2/webhooks",
      "params": [
        {
          "name": "offset",
          "type": "float",
          "location": "query",
          "description": "Number of items that should be skipped at the start. The default value is `0`."
        },
        {
          "name": "limit",
          "type": "float",
          "location": "query",
          "description": "Maximum number of items to return. The default value as well as the maximum is `1000`."
        },
        {
          "name": "desc",
          "type": "bool",
          "location": "query",
          "description": "If `true` or `1` then the objects are sorted by the `createdAt` field in descending order."
        }
      ]
    },
    {
      "name": "webhooks_get-webhookid",
      "description": "Gets webhook object with all details. Required: webhookId. Returns the WebhookResponse.",
      "method": "GET",
      "path": "/v2/webhooks/{webhookId}",
      "params": [
        {
          "name": "webhookId",
          "type": "string",
          "location": "path",
          "description": "Webhook ID.",
          "required": true
        }
      ]
    },
    {
      "name": "webhooks_post",
      "description": "Creates a new webhook with settings provided by the webhook object passed as JSON in the payload. The response is the created webhook object. To avoid duplicating a webhook, use the `idempotencyKey` parameter in the request body. Multiple calls to create a webhook with the same `idempotencyKey` will only create the webhook with the first call and return the existing webhook on subsequent calls. Idempotency keys must be unique, so use a UUID or another random string with enough entropy. To assign the new webhook to an Actor or task, the request body must contain `requestUrl`, `eventTypes`, and `condition` properties. * `requestUrl` is the webhook's target URL, to which data is sent as a POST request with a JSON payload. * `eventTypes` is a list of events that will trigger the webhook, e.g. when the Actor run succeeds. * `condition` should be an object containing the ID of the Actor or task to which the webhook will be assigned. * `payloadTemplate` is a JSON-like string, whose syntax is extended with the use of variables. * `headersTemplate` is a JSON-like string, whose syntax is extended with the use of variables. Following values will be re-written to defaults: 'host', 'Content-Type', 'X-Apify-Webhook', 'X-Apify-Webhook-Dispatch-Id', 'X-Apify-Request-Origin' * `description` is an optional string. * `shouldInterpolateStrings` is a boolean indicating whether to interpolate variables contained inside strings in the `payloadTemplate` ``` 'isAdHoc' : false, 'requestUrl' : 'https://example.com', 'eventTypes' : [ 'ACTOR.RUN.SUCCEEDED', 'ACTOR.RUN.ABORTED' ], 'condition' : { 'actorId': '5sTMwDQywwsLzKRRh', 'actorTaskId' : 'W9bs9JE9v7wprjAnJ' }, 'payloadTemplate': '', 'headersTemplate': '', 'description': 'my awesome webhook', 'shouldInterpolateStrings': false, ``` **Important**: The request must specify the `Content-Type: application/json` HTTP header. Required: condition, eventTypes, requestUrl. Optional: description, doNotRetry, headersTemplate (plus 5 more). Returns the new WebhookResponse.",
      "method": "POST",
      "path": "/v2/webhooks",
      "params": [
        {
          "name": "condition",
          "type": "object",
          "location": "body",
          "description": "Condition",
          "required": true
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "Description"
        },
        {
          "name": "doNotRetry",
          "type": "bool",
          "location": "body",
          "description": "Do not retry"
        },
        {
          "name": "eventTypes",
          "type": "array",
          "location": "body",
          "description": "Event types",
          "required": true
        },
        {
          "name": "headersTemplate",
          "type": "string",
          "location": "body",
          "description": "Headers template"
        },
        {
          "name": "idempotencyKey",
          "type": "string",
          "location": "body",
          "description": "Idempotency key"
        },
        {
          "name": "ignoreSslErrors",
          "type": "bool",
          "location": "body",
          "description": "Ignore ssl errors"
        },
        {
          "name": "isAdHoc",
          "type": "bool",
          "location": "body",
          "description": "Is ad hoc"
        },
        {
          "name": "payloadTemplate",
          "type": "string",
          "location": "body",
          "description": "Payload template"
        },
        {
          "name": "requestUrl",
          "type": "string",
          "location": "body",
          "description": "Request url",
          "required": true
        },
        {
          "name": "shouldInterpolateStrings",
          "type": "bool",
          "location": "body",
          "description": "Should interpolate strings"
        }
      ]
    },
    {
      "name": "webhooks_put",
      "description": "Updates a webhook using values specified by a webhook object passed as JSON in the POST payload. If the object does not define a specific property, its value will not be updated. The response is the full webhook object as returned by the [Get webhook](#/reference/webhooks/webhook-object/get-webhook) endpoint. The request needs to specify the `Content-Type: application/json` HTTP header! When providing your API authentication token, we recommend using the request's `Authorization` header, rather than the URL. ([More info](#/introduction/authentication)). Required: webhookId. Optional: condition, description, doNotRetry (plus 7 more). Returns the updated WebhookResponse.",
      "method": "PUT",
      "path": "/v2/webhooks/{webhookId}",
      "params": [
        {
          "name": "webhookId",
          "type": "string",
          "location": "path",
          "description": "Webhook ID.",
          "required": true
        },
        {
          "name": "condition",
          "type": "string",
          "location": "body",
          "description": "Condition"
        },
        {
          "name": "description",
          "type": "string",
          "location": "body",
          "description": "Description"
        },
        {
          "name": "doNotRetry",
          "type": "bool",
          "location": "body",
          "description": "Do not retry"
        },
        {
          "name": "eventTypes",
          "type": "array",
          "location": "body",
          "description": "Event types"
        },
        {
          "name": "headersTemplate",
          "type": "string",
          "location": "body",
          "description": "Headers template"
        },
        {
          "name": "ignoreSslErrors",
          "type": "bool",
          "location": "body",
          "description": "Ignore ssl errors"
        },
        {
          "name": "isAdHoc",
          "type": "bool",
          "location": "body",
          "description": "Is ad hoc"
        },
        {
          "name": "payloadTemplate",
          "type": "string",
          "location": "body",
          "description": "Payload template"
        },
        {
          "name": "requestUrl",
          "type": "string",
          "location": "body",
          "description": "Request url"
        },
        {
          "name": "shouldInterpolateStrings",
          "type": "bool",
          "location": "body",
          "description": "Should interpolate strings"
        }
      ]
    },
    {
      "name": "webhooks_dispatches_webhook-webhook-get",
      "description": "Gets a given webhook's list of dispatches. Required: webhookId. Returns the ListOfWebhookDispatchesResponse.",
      "method": "GET",
      "path": "/v2/webhooks/{webhookId}/dispatches",
      "params": [
        {
          "name": "webhookId",
          "type": "string",
          "location": "path",
          "description": "Webhook ID.",
          "required": true
        }
      ]
    },
    {
      "name": "webhooks_test_webhook-post",
      "description": "Tests a webhook. Creates a webhook dispatch with a dummy payload. Required: webhookId. Returns the new TestWebhookResponse.",
      "method": "POST",
      "path": "/v2/webhooks/{webhookId}/test",
      "params": [
        {
          "name": "webhookId",
          "type": "string",
          "location": "path",
          "description": "Webhook ID.",
          "required": true
        }
      ]
    }
  ]
}
